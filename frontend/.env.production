# 正式環境（npm run build 會讀這個檔）
#
# 規則與說明見 .env.development，這裡只列值。
#

# 前端與後端同網域部署，用相對路徑即可（部署方式見 tech/quality/50-部署上線）。
VITE_API_BASE=/api

# 空字串代表「跟目前網頁同一個 host，並依 http/https 自動選 ws/wss」，
# 由 services/realtime/socket.js 組出完整網址。
VITE_WS_URL=
