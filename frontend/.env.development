# 開發環境（npm run dev 會讀這個檔）
#
# ★ 三條規則，違反任何一條都不會報錯，只會安靜地出錯：
#
#   1. 變數名一定要 VITE_ 開頭，否則前端讀不到。
#      Vite 預設把所有環境變數擋在前端外面，只有 VITE_ 開頭的才放行。
#
#   2. 反過來說，VITE_ 開頭的東西等於公開。打包後就寫在 JS 檔裡，
#      按 F12 就看得到。所以這裡永遠不要放密碼、API 金鑰、資料庫連線字串。
#
#   3. 讀出來永遠是「字串」，沒有布林值。要當布林用一定要自己比對，
#      例如 `import.meta.env.VITE_SOMETHING === 'true'`——直接 if 的話 "false" 也是真值。
#
# ★ 改完這個檔要重開 dev server。
#   這些值是 build 時被替換進程式碼的，不是執行時去查表。
#
# ★ 要覆蓋成自己機器的設定（例如後端開在別的 port），
#   不要改這個檔，另外開 .env.development.local——
#   它的優先權比這個檔高，而且 .gitignore 的 *.local 已經蓋掉它，
#   不會被 commit 出去干擾別人。

# 後端位址。Spring Boot 預設 8080，前端 dev server 在 5173，
# 跨來源由後端的 CORS 設定放行（L0 負責，見 05 §1.4）。
VITE_API_BASE=http://localhost:8080/api

# WebSocket 位址。原生 WebSocket，不走 SockJS（04-API規格 §3.4）。
VITE_WS_URL=ws://localhost:8080/ws
