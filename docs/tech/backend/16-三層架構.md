# 三層架構：Controller / Service / Repository

**難度** ★★☆☆☆　**用在哪些模組** 所有後端工作　**哪幾週** 第 1 週

## 一句話

把後端分成**接單的、做菜的、拿食材的**三層，各做各的事。

## 想像一下

火鍋店的分工：

| 角色 | 做什麼 | 不做什麼 |
|---|---|---|
| **服務生** | 接單、確認客人講清楚了、把菜端出去 | 不煮菜、不進冰箱 |
| **廚師** | 真正決定怎麼煮、幾分熟、要不要加料 | 不接待客人、不管冰箱怎麼擺 |
| **備料員** | 從冰箱拿食材、放回去 | 不決定要煮什麼 |

如果服務生一邊接單一邊煮菜一邊翻冰箱，整間店會亂掉。

後端也一樣：

| 層 | 對應 | 負責 | **絕對不做** |
|---|---|---|---|
| **Controller** | 服務生 | 接請求、檢查格式、回傳結果 | **不寫商業邏輯、不碰資料庫** |
| **Service** | 廚師 | 商業規則、計算、交易邊界 | **不知道 HTTP 存在** |
| **Repository** | 備料員 | 對資料庫讀寫 | **不做判斷** |

## 三層長什麼樣

**Controller —— 薄薄一層**

```java
@RestController
@RequestMapping("/api/dining-sessions")
@RequiredArgsConstructor
public class OrderController {

    private final OrderService orderService;

    @PostMapping("/me/orders")
    public OrderTicketDto submit(@RequestHeader("X-Session-Token") String token,
                                 @Valid @RequestBody SubmitOrderRequest request) {
        return orderService.submitOrder(token, request);
    }
}
```

**就這樣，三行。** Controller 的工作只有：拿參數、丟給 Service、回傳結果。

**Service —— 真正的重點**

```java
@Service
@RequiredArgsConstructor
public class OrderService {

    private final DiningSessionRepository sessionRepo;
    private final OrderTicketRepository ticketRepo;
    private final InventoryService inventoryService;

    @Transactional
    public OrderTicketDto submitOrder(String token, SubmitOrderRequest req) {
        DiningSession session = sessionRepo.findByTokenForUpdate(token)   // 鎖住這一列，跟櫃檯結清搶同一把鎖
            .orElseThrow(() -> new BusinessException("INVALID_SESSION_TOKEN"));

        if (session.getStatus() != SessionStatus.OPEN) {
            throw new BusinessException("SESSION_CLOSED", "這桌已經結帳，不能再加點");
        }

        // 檢查必選選項、檢查鍋底、扣庫存、建立點餐單、算金額 ...
    }
}
```

**Repository —— Spring 幫你寫好**

```java
public interface OrderTicketRepository extends JpaRepository<OrderTicket, Long> {
    List<OrderTicket> findByDiningSessionIdOrderBySequenceNo(Long sessionId);
}
```

你只要**宣告方法名字**，Spring Data JPA 會自動幫你產生 SQL。（詳見 [ORM 與 JPA](../database/25-ORM與JPA.md)）

## 為什麼要分

| 好處 | 說明 |
|---|---|
| **找得到東西** | 五個人協作，「金額怎麼算的」一定在 Service，不用全專案搜尋 |
| **可以測試** | Service 不依賴 HTTP，可以直接寫[單元測試](../quality/47-單元測試.md) |
| **交易邊界清楚** | `@Transactional` 貼在 Service，一個方法就是一個交易 |
| **可以重用** | 顧客自己點餐和櫃檯代客加點，用的是同一個 Service 方法 |

## 資料在三層之間怎麼傳

```
前端 --JSON--> Controller --Request DTO--> Service --Entity--> Repository --SQL--> 資料庫
前端 <--JSON-- Controller <--Response DTO-- Service <--Entity-- Repository <--
```

**重點**：Controller 對外用 [DTO](17-Entity與DTO.md)，Repository 對內用 Entity。Service 負責在中間轉換。

**絕對不要把 Entity 直接回傳給前端**——原因見下一頁。

## 15 分鐘動手小練習

把你的 Hello 專案改成三層：

1. `GreetingRepository`（先假裝有資料庫，回傳固定字串）
2. `GreetingService`（決定早上說早安、晚上說晚安）
3. `GreetingController`（只負責接請求）

```java
@Repository
public class GreetingRepository {
    public String findStoreName() { return "老石頭火鍋"; }
}

@Service
@RequiredArgsConstructor
public class GreetingService {
    private final GreetingRepository repo;
    public String greet(String name) {
        String prefix = LocalTime.now().getHour() < 12 ? "早安" : "午安";
        return prefix + "，" + name + "！歡迎光臨" + repo.findStoreName();
    }
}
```

**練習重點**：時間判斷的邏輯放在 Service，不是 Controller。這個習慣要從第一天養成。

## 你會遇到的坑

**① Controller 裡寫邏輯**
最常見。一開始只是加個 `if`，三週後 Controller 變成 300 行。
→ **Controller 超過 10 行就該檢討。**

**② Service 裡出現 HttpServletRequest**
代表你的 Service 被 HTTP 綁死了，不能重用也不好測試。

**③ Repository 裡做判斷**
Repository 只負責拿資料，不判斷。

**④ 把 Entity 直接回傳**
會出現 lazy loading 錯誤、洩漏不該給前端的欄位、改資料庫就打壞 API。

**⑤ 一個 Service 一千行**
Service 也可以拆。`OrderService`、`OrderValidationService`、`OrderPricingService`。

## 常見錯誤訊息對照

| 你會看到 | 中文意思 | 怎麼修 |
|---|---|---|
| `failed to lazily initialize a collection` | Entity 離開交易後才被讀取 | 在 Service 裡轉成 DTO，不要把 Entity 丟給 Controller |
| `Infinite recursion (StackOverflowError)` | 兩個 Entity 互相參照，轉 JSON 時無限循環 | 同上，用 DTO |
| `No property 'xxx' found for type` | Repository 方法名字拼錯 | 方法名要對應 Entity 的欄位名 |

## 術語對照表

| 白話 | 正式名稱 |
|---|---|
| 服務生層 | Controller / Presentation Layer |
| 廚師層 | Service / Business Layer |
| 備料層 | Repository / Data Access Layer（DAO） |
| 對外的資料格式 | DTO |
| 對應資料表的物件 | Entity |

## 自我檢核

1. 「服務費 10%」的計算應該寫在哪一層？
2. Controller 可以直接呼叫 Repository 嗎？為什麼不行？
3. `@Transactional` 應該貼在哪一層？
4. 為什麼不能把 Entity 直接回傳給前端？

## 學習資源

| 資源 | 語言 | 難度 | 時間 |
|---|---|---|---|
| [Spring Boot 零基礎入門 — 分層架構篇章](https://ithelp.ithome.com.tw/users/20151036/ironman/6130) | 繁中 | 入門 | 20 分 |

## 相關頁面

[依賴注入](15-依賴注入.md)　[Entity 與 DTO](17-Entity與DTO.md)　[交易 Transaction](../database/27-交易Transaction.md)
