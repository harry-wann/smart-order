# 模組 6：結帳

**難易度** ★★☆☆☆　**里程碑** 第 4 週

## 這個模組在做什麼

算清楚這桌總共多少錢，收款，然後把桌子放回可用狀態。

**基礎版是客人到櫃檯結帳**：櫃檯在 S-03 桌位詳情看帳單，結帳區有三顆付款按鈕「現金／信用卡／條碼」，三顆都先跳同一個 5 秒倒數（S-03b，可取消），倒數到 0 才真的送出，成功後約 1.5 秒自動關閉。
客人在自己手機上付款（「我要結帳」→ C-09～C-11）是**進階 A5**（6.E7），基礎全綠燈後才動工。

**這裡要記得我們的定位：半輔助工具。** 系統管的是「點什麼、出到哪、坐多久」，**不管錢怎麼進抽屜**。所以我們**不做**找零、交班、日結、稽核、發票、折讓——那些留給店家原本的收銀機。

## 難易度為什麼只有兩顆星

沒有複雜演算法、沒有即時推播的難點，併發也只有一處（跟開桌同一招：列鎖）。主要是：

- 金額計算要正確（用 `BigDecimal`）
- 結清與加點要搶同一把列鎖，**金額比對不上就擋**（見問題 1）
- 櫃檯結清只是標記；線上付款（進階 A5）也是模擬的，不串接任何真實金流

**唯一要小心的是「不要把範圍做大」。** 結帳很容易越做越像 POS 系統。

## 基礎必做功能

| # | 功能 | 驗收條件 | 狀態 |
|---|---|---|---|
| 6.1 | 帳單計算 | 小計 → 服務費 10% → 合計，全部用 `BigDecimal` | ☐ |
| 6.2 | 帳單明細 | 依點餐單分組，含每項的選項與小計（櫃檯在 S-03 看；A5 的 C-09 用同一份） | ☐ |
| 6.3 | **結帳防呆：金額比對** | 結清（與進階的線上付款）都帶 `expectedTotal`；後端在交易裡 `SELECT ... FOR UPDATE` 鎖住 `dining_session` 重算，不相等回 `409 BILL_CHANGED` 與最新帳單 | ☐ |
| 6.7 | **櫃檯結清** | S-03 結帳區三顆按鈕 **現金／信用卡／條碼**（`CASH`／`CARD`／`BARCODE`）。點任何一顆 → S-03b 同一個倒數對話框，標題跟著方式變（現金「收款中，請把現金放進收銀機」、信用卡「信用卡付款中」、條碼「條碼付款中」）＋大數字倒數 5 → 0（每秒一格）＋金額＋小字「本系統為教學專題，付款為模擬流程」＋ghost「取消」；倒數到 0 才送 `POST /api/admin/dining-sessions/{id}/settle`；成功換成勾勾「結帳完成」，**約 1.5 秒後自動關閉**回 S-02（不用按）。**不做失敗機率**、不管找零 | ☐ |
| 6.9 | 桌位轉待清理 | 結清成功後 `dining_session` 轉 `PAID`、桌位轉 `CLEANING`（同一個交易） | ☐ |
| 6.10 | 權杖失效＋同桌顯示已結帳 | 該桌用餐權杖立刻失效、整桌購物車刪掉（還沒送出的品項直接捨棄，不算錢）；推 `SESSION_CLOSED`（`reason: PAID`），同桌手機的 C-04 切到「已結帳」狀態 | ☐ |
| 6.11 | 防重複送出 | 倒數到 0 送出後，對話框按鈕停用、不能再取消；兩邊同時結帳由列鎖擋，後到的拿 `409 SESSION_CLOSED` | ☐ |

> **6.4（取消結帳）刪除，空號不重編**——沒有鎖定狀態，就沒有「解鎖」這件事。
> **原 6.5、6.6、6.8、6.12 移到 6.E7（A5 顧客線上結帳），編號留空。**
> 客人預設到櫃檯結帳；C-08 底部只留一行「用餐完畢請到櫃檯結帳」，服務鈴也沒有「我要結帳」。
> 付款方式從「現金／模擬支付」改成「現金／信用卡／條碼」（2026-09-16），`payment.method` 沒有 `MOCK_PAY` 了。

> **S-03 結帳區的「會員（選填）」那一塊是進階 8.10 的櫃檯入口**（A1／A6 的前提）：輸入手機 → 查詢 → 顯示「陳小美・1998/04/21」讓櫃檯唸給客人確認 →
> 「確認是本人」才把 `memberId` 放進 settle 的 body，同一個交易寫進 `dining_session.member_id`。不做 8.10 就整塊拿掉，settle 不帶 `memberId`。細節見 [M8](M8-會員.md) 8.10。

> **結帳時整桌購物車還有東西**：結清的同一個交易裡直接捨棄、不算錢（6.10）。**櫃檯不用管購物車**：S-03／S-03b 不顯示任何購物車提示，
> `GET /api/admin/dining-sessions/{id}` 也不回購物車資訊，櫃檯只看已經送出的單。客人要那幾樣，就要在結帳前自己送出（或請櫃檯代客加點）。

> **S-03 還有一顆「取消該次用餐」**（僅店長，[M2](M2-桌位開桌與候位.md) 2.14），跟結帳是兩條路：沒點過餐才能取消，
> 點過餐就只能結帳（`409 SESSION_HAS_ORDERS`）。取消推的也是 `SESSION_CLOSED`，但 `reason: CANCELLED`，手機不顯示「已結帳」面板、直接回 C-00。

## 延伸加分功能

| # | 功能 | 說明 |
|---|---|---|
| 6.E1 | 發票模擬 | 產生假的發票號碼與 QR |
| 6.E2 | **營收報表 S-12** | **已有設計稿**（§4.1 的 A4）：近 7 天營收、時段分布、熱門品項、匯出 CSV。6.E3／6.E4 已經畫在同一頁裡 |
| 6.E3 | 翻桌率統計 | 平均用餐時間、每桌每日翻幾次（S-12 那一頁的統計列） |
| 6.E4 | 熱門品項報表 | （S-12 那一頁的下半） |
| 6.E5 | 優惠券與點數折抵（**A6 的一部分**） | 只能在 C-09（A5）使用。**券和點數可以同一次一起用**：先扣優惠券（一次最多一張）→ 再用點數（1 點折 1 元，最多折到 0 元）。付款的 `expectedTotal` 帶**折抵前**的合計，比對通過才套用折抵。櫃檯結帳（S-03）要用券或點數是 A6 的延伸，這次沒畫 |
| 6.E7 | **顧客線上結帳 C-09～C-11** | **已有設計稿**（§4.1 的 A5）。收原 6.5 模擬行動支付（三顆按鈕：Apple Pay／Google Pay／LINE Pay 樣式，2 秒動畫，95% 成功）、6.6 付款失敗處理（5% 失敗 → 顯示原因＋可重試，狀態不變）、6.8 電子收據（交易編號、金額、時間、品項）、6.12 教學專題聲明（付款頁標示「本系統為教學專題，付款為模擬流程」）。付款一樣帶 `expectedTotal`，走 6.3 的金額比對；送出中按鈕停用＋全頁遮罩 |

## 明確不做

**這些請不要動工**：找零計算、交班日結、現金核對、操作稽核紀錄、退菜折讓的財務流程、真實發票開立、**分帳**、營業時間管理。

（分帳在共鍋情境下其實很合理，但我們在規劃階段決定不做。想做的話列為 6.E6。）

## 會用到的技術

| 技術 | 為什麼需要 | 難度 |
|---|---|---|
| [交易 Transaction](../../tech/database/27-交易Transaction.md) | 結清成功要同時改三個狀態 | ★★★☆☆ |
| [Java 與 JDK（BigDecimal）](../../tech/backend/12-Java與JDK.md) | 算錢不能用 double | ★☆☆☆☆ |
| [鎖與併發](../../tech/database/28-鎖與併發.md) | 結清與加點搶同一列的鎖 | ★★★☆☆ |
| [HTTP 狀態碼](../../tech/web/08-HTTP狀態碼.md) | 409 金額變動／已結帳、422 付款失敗（A5） | ★☆☆☆☆ |
| [全域例外處理](../../tech/backend/18-全域例外處理.md) | 統一錯誤格式 | ★★☆☆☆ |
| [fetch 串接後端 API](../../tech/frontend/40-fetch串接API.md) | 防重複送出 | ★★★☆☆ |
| [WebSocket 與 STOMP](../../tech/realtime/41-WebSocket與STOMP.md) | 結清後推 `SESSION_CLOSED` 給同桌手機 | ★★★☆☆ |

---

## 問題思考與解決思路

### 問題 1：結帳到一半有人加點

**情境**
櫃檯在 S-03 看到這桌合計 1562，正要按「信用卡」；同一時間，桌上的朋友在手機上又加了一盤肉。

**如果不處理**
櫃檯收的是 1562，實際帳單已經變成 1914。**錢收了，金額卻不對。**

**舊做法：先鎖再付（已拿掉）**
舊版有一個 `CHECKOUT_PENDING` 狀態：按「我要結帳」就把整桌鎖住不能加點，還要配一顆「取消結帳」解鎖。
代價是多一個狀態、兩支 API、一個 WebSocket 事件；而且基礎版客人是走去櫃檯結帳，
手機上根本沒有「開始結帳」這個時間點可以鎖。

**正確做法：金額比對 + 列鎖**

畫面上看到多少，就把那個數字一起送上來（`expectedTotal`）。
後端在同一個交易裡鎖住這筆 `dining_session`、重算一次，不一樣就擋下來。

```java
@Transactional
public SettleResult settle(Long sessionId, SettleRequest req) {
    // SELECT ... FOR UPDATE：跟送出點餐搶同一列的鎖
    DiningSession session = sessionRepo.findByIdForUpdate(sessionId)
        .orElseThrow(() -> new BusinessException("NOT_FOUND", "查無此筆用餐", NOT_FOUND));

    if (session.getStatus() != SessionStatus.OPEN) {
        throw new BusinessException("SESSION_CLOSED", "這桌已經結帳，不能再加點", CONFLICT)
            .with("reason", session.getStatus().name());          // PAID 或 CANCELLED，前端靠它決定顯示什麼
    }

    CheckoutAmount amount = calculator.calculate(session);        // 重算，不相信畫面
    if (amount.total().compareTo(req.expectedTotal()) != 0) {     // 用 compareTo，見問題 2
        throw new BusinessException("BILL_CHANGED", "金額有變動，請重新確認", CONFLICT)
            .with("bill", toBillDto(session, amount));             // 附最新帳單，前端直接重畫
    }

    if (req.memberId() != null) {                                 // 進階 8.10 的櫃檯入口：客人確認是本人才會帶
        session.attachMember(req.memberId());                     // 已綁了別的會員 → 409 ALREADY_ATTACHED
    }

    paymentRepo.save(Payment.settled(session, req.method(), amount.total()));  // CASH、CARD 或 BARCODE
    closeSession(session, amount);
    return toResult(session);
}

// 收尾：櫃檯結清與（進階 A5）線上付款成功共用，兩邊不要各寫一份
private void closeSession(DiningSession session, CheckoutAmount amount) {
    session.markPaid(amount);                                     // → PAID，用餐權杖一起失效
    session.getTable().setStatus(TableStatus.CLEANING);
    cartRepo.deleteBySessionId(session.getId());                  // 還沒送出的購物車直接捨棄，不算錢
    var event = new WsEvent("SESSION_CLOSED", Map.of("sessionId", session.getId(), "reason", "PAID"));
    events.publishAfterCommit("/topic/session/" + session.getId(), event);  // 同桌手機切到「已結帳」
    events.publishAfterCommit("/topic/counter", event);                     // 櫃檯更新桌況
}
```

送出點餐那一邊，也在交易裡鎖同一列：

```java
// OrderService.submitOrder()，同一個 @Transactional 裡、扣庫存之前
DiningSession session = sessionRepo.findByIdForUpdate(sessionId).orElseThrow(...);
if (session.getStatus() != SessionStatus.OPEN) {
    throw new BusinessException("SESSION_CLOSED", "這桌已經結帳，不能再加點", CONFLICT)
        .with("reason", session.getStatus().name());   // 409 回應也帶 reason（04 §1.2）
}
```

**兩邊搶同一把鎖，所以一定一先一後：**

| 誰先拿到鎖 | 結果 |
|---|---|
| 加點先 | 單成立、合計變了 → 櫃檯的結清拿到 `409 BILL_CHANGED`，畫面換成新金額，再按一次 |
| 結清先 | 這桌已是 `PAID` → 加點拿到 `409 SESSION_CLOSED`；同桌手機收到 `SESSION_CLOSED`，C-04 顯示「已結帳」 |

**櫃檯結清和客人線上付款（進階 A5）同時按**也是同一個道理：後到的拿 `409 SESSION_CLOSED`，不會收兩次錢。

> 只鎖一邊等於沒鎖。結清、付款、送出點餐三個地方都要 `FOR UPDATE` **同一筆** `dining_session`。

---

### 問題 2：金額算出來差一塊錢

**情境**
小計 1420，服務費 10%，合計應該是 1562。但系統顯示 1561.9999999。

**原因**
用了 `double` 或 `float`。電腦的浮點數沒辦法精確表示 0.1。

```java
System.out.println(0.1 + 0.2);   // 0.30000000000000004
```

**正確做法：從頭到尾用 BigDecimal**

```java
public CheckoutAmount calculate(DiningSession session) {
    BigDecimal subtotal = session.getTickets().stream()
        .filter(t -> t.getStatus() != TicketStatus.CANCELLED)
        .flatMap(t -> t.getItems().stream())
        .filter(i -> i.getStatus() != ItemStatus.CANCELLED)
        .map(OrderItem::getLineTotal)
        .reduce(BigDecimal.ZERO, BigDecimal::add);

    BigDecimal fee = subtotal.multiply(SERVICE_FEE_RATE)
                             .setScale(2, RoundingMode.HALF_UP);

    return new CheckoutAmount(subtotal, fee, subtotal.add(fee));
}
```

三個重點：

1. **資料庫欄位用 `DECIMAL(10,2)`**，不要 `FLOAT`
2. **建立 BigDecimal 用字串**：`new BigDecimal("0.10")` 不是 `new BigDecimal(0.10)`
3. **每一步都 `setScale(2, HALF_UP)`**，明確指定進位規則

**比較兩個 BigDecimal 要用 `compareTo`**，`equals` 會認為 `142.0` 和 `142.00` 不相等。

---

### 問題 3：模擬付款的界線在哪（進階 A5）

**情境**
規格寫「模擬 Apple Pay / Google Pay / LINE Pay」。

**如果不寫清楚會發生什麼**
一定會有組員跑去研究真的串接文件。LINE Pay 要商家申請、Apple Pay 要開發者帳號 + 正式網域——**浪費三到五天，而且做不出來。**

**明確定義（請貼進群組）**

> 付款 = 一個假的付款畫面 → 客人選一種方式（三顆按鈕，只是圖示不同）→ 前端顯示 2 秒處理動畫 → 後端建立一筆 `payment` 紀錄，隨機 95% 成功／5% 失敗 → 回傳交易編號。
> **不串接任何真實金流 API。不申請任何開發者帳號。**

```java
@Transactional
public PaymentDto pay(PayRequest req) {
    // 先走問題 1 的列鎖 + 狀態檢查 + expectedTotal 比對（跟櫃檯結清共用）
    DiningSession session = lockAndVerify(req.diningSessionId(), req.expectedTotal());
    CheckoutAmount amount = calculator.calculate(session);

    boolean success = random.nextInt(100) < 95;      // 95% 成功
    Payment payment = paymentRepo.save(Payment.of(session, req.method(), amount.total(),
        success ? PayStatus.SUCCESS : PayStatus.FAILED,
        "MOCK-" + LocalDate.now().format(BASIC_ISO_DATE) + "-" + nextSeq()));

    if (success) {                                    // 成功：收尾跟櫃檯結清完全一樣（04 §4.9）
        closeSession(session, amount);                // PAID、桌位 CLEANING、權杖失效、刪購物車、推 SESSION_CLOSED
    }
    return toDto(payment);   // FAILED 由 Controller 轉成 422 PAYMENT_FAILED
}
```

> **失敗不要用「丟例外」離開這個方法。** `@Transactional` 遇到 RuntimeException 會整個回滾，
> 剛存進去的 `FAILED` 紀錄也會一起消失，事後查不到失敗過。用回傳值表示失敗，交給 Controller 回 422。

**為什麼要留 5% 失敗？** 因為 demo 時可以展示錯誤處理。一個「永遠成功」的付款流程看不出你有沒有想過失敗的情況。

**付款頁要明確標示「本系統為教學專題，付款為模擬流程，不會產生實際交易」。** 誠實，而且評審會欣賞。

---

### 問題 4：人數不進帳單計算，那收集人數幹嘛？

**情境**
我們的計價是「一鍋一價 + 單點」、無低消。所以：

```
小計 = Σ(品項 × 數量)
服務費 = 小計 × 10%
合計 = 小計 + 服務費
```

**人數完全不參與金額計算。**

**那為什麼還要輸入大人／小孩人數？** 四個用途：

| 用途 | 說明 |
|---|---|
| **座位安排** | 櫃檯配桌、訂位的 best-fit |
| **分帳均分** | 顯示「每人約 $521」（即使不做分帳功能，顯示參考值也有用） |
| **兒童友善推薦** | 有小孩時把辣度 0 的品項排前面 |
| **客單價統計** | 營收 ÷ 人數 |

**這點一定要寫進文件**，不然組員會問「為什麼要收集人數卻不用」，或者更糟——**自己加一個人頭費的計算進去**。

---

### 問題 5：櫃檯按錯付款方式怎麼辦

**情境**
客人說要刷卡，櫃檯手滑按到「現金」。

**如果一按就送出**
帳就記成現金了。這一版沒有「改付款方式」也沒有退款流程，只能進資料庫改。

**正確做法：倒數 5 秒，倒數中可以取消**（S-03b）

點任何一顆付款按鈕，先跳同一個倒數對話框：大數字 5 → 0＋金額＋「取消」，標題跟著方式變——
現金「收款中，請把現金放進收銀機」、信用卡「信用卡付款中」、條碼「條碼付款中」。**倒數到 0 才送 settle**；
倒數期間按「取消」，什麼都不會發生。成功後換成勾勾「結帳完成」，**約 1.5 秒後自動關閉**回 S-02，櫃檯不用多按一下。

> **現金也要倒數嗎？** 要（2026-09-16 定案）。三顆走同一個流程，程式只有一條路；現金的倒數剛好是「把錢放進收銀機」的時間。

```jsx
// 倒數只在前端：取消不需要任何 API，因為還沒送出任何東西
useEffect(() => {
  if (!paying) return;
  if (count === 0) { settle(paying.method); return; }      // 到 0 才送，送出中按鈕停用（6.11）；成功後 setTimeout 1500 自動關閉
  const t = setTimeout(() => setCount(c => c - 1), 1000);
  return () => clearTimeout(t);                             // 按「取消」→ setPaying(null)，計時器被清掉
}, [paying, count]);
```

> **倒數途中有人加點**：倒到 0 送出時照樣走問題 1 的 `expectedTotal` 比對，拿到 `409 BILL_CHANGED` →
> 對話框關閉、帳單刷新、提示「剛剛有人加點，金額更新了」，櫃檯再按一次。

**為什麼櫃檯結帳不做失敗機率？** 櫃檯是真的收到錢、刷過卡才按的，系統只是記錄；
「付款失敗、可重試」是客人自己在手機上付款（A5）才有的情境。

---

## 六週切分

| 週 | 這個模組要交出什麼 |
|---|---|
| **W1** | Flyway 建好 `payment`；服務費率等設定放進 `application.yml` |
| **W2** | 金額計算 Service（含單元測試） |
| **W3** | ★ 櫃檯結清 `POST /api/admin/dining-sessions/{id}/settle`（含列鎖與 `expectedTotal` 比對）；S-03 下半的結帳區（現金／信用卡／條碼三顆）與 S-03b 倒數對話框；**主流程全綠燈** |
| **W4** | `BILL_CHANGED` 的前端刷新；跟 M3 一起測「結清的同時有人加點」（送出點餐那邊的 `SESSION_CLOSED` 檢查在 M3 W3 就做好，兩邊同一週鎖同一列才成立） |
| **W5** | 權杖失效驗證；WebSocket `SESSION_CLOSED` 通知同桌（`reason: PAID` → C-04「已結帳」狀態；`CANCELLED` → 直接回 C-00，跟 M2 2.14 一起測） |
| **W6** | 不寫新功能。測金額計算的邊界（0 元、只有續加品）與「結清的同時有人加點」 |
| **有餘力才做** | 6.E7 顧客線上結帳（A5）：`GET /api/dining-sessions/me/bill`、`POST /api/payments`（模擬付款 Service、失敗重試）、C-09／C-10／C-11；8.10 的櫃檯入口（S-03 會員查詢、settle 帶 `memberId`）。**基礎全綠燈之前不要動它** |

## 常見卡關

| 卡點 | 解法 |
|---|---|
| **金額差一塊錢** | 檢查有沒有用 `double`；檢查 `setScale` 的進位規則 |
| **BigDecimal 比較不相等** | 用 `compareTo` 或 `isEqualByComparingTo`，不要用 `equals`；金額比對用 `equals` 會每次都回 `BILL_CHANGED` |
| **取消的品項也被算進去** | 計算時要 `filter` 掉 `CANCELLED` |
| **結清成功但桌位沒轉狀態** | 三個狀態要在同一個 `@Transactional` 裡改 |
| **櫃檯連按兩次結清** | 前端 `disabled={submitting}`；後端第二次會拿到 `409 SESSION_CLOSED` |
| **結帳完成的對話框一直停著** | 成功後要自己 `setTimeout` 約 1.5 秒關閉並回 S-02，不是等櫃檯按 |
| **結帳後購物車的東西還在** | `closeSession` 漏了刪 `cart_item`；A5 線上付款成功也走同一個 `closeSession` |
| **按了取消還是結帳了** | 一按就送了 settle、倒數只是動畫；要倒數到 0 才送（問題 5） |
| **取消用餐後手機顯示「已結帳」** | `SESSION_CLOSED` 沒看 `reason`，`CANCELLED` 要直接回 C-00 |
| **結清一直回 `BILL_CHANGED`** | 前端送的是舊金額；收到 409 要用回傳的最新帳單重畫，再讓櫃檯按一次 |
| **結清時有人加點卻沒被擋** | 送出點餐沒有 `FOR UPDATE` 同一筆 `dining_session`，只鎖一邊等於沒鎖 |
| **付款失敗紀錄查不到**（A5） | 失敗時丟了例外，交易回滾把 `FAILED` 紀錄也收掉了，見問題 3 |

## 驗收 Demo 腳本

### 基礎（一定要演完）

1. 手機點三樣東西（含一個有選項價差的）→ 送出；C-08 底部看得到「用餐完畢請到櫃檯結帳」
2. 櫃檯 S-03 點開這桌：品項明細、小計、服務費 10%、合計；「小計 N 項」的 N 是已送出品項的份數加總
3. **櫃檯畫面先不動，手機再加點一盤** → 櫃檯按「信用卡」→ 倒數 5 → 0 → **對話框關閉，提示「剛剛有人加點，金額更新了」，合計換成新金額**
4. 按「現金」→ 倒數中按「取消」→ **什麼都沒發生**；這時在手機加一樣東西**但不送出** → S-03 **沒有任何購物車提示**，合計也不變；再按一次「現金」等倒數到 0 → 勾勾「結帳完成」→ **約 1.5 秒後自動關閉**回 S-02（對話框標題是「收款中，請把現金放進收銀機」）
5. 桌況變「待清理」；**兩支手機的 C-04 同時變成「已結帳」**，菜單變暗、不能再加點；第 4 步沒送出的那一樣跟著購物車一起捨棄，不算錢
6. 手機按「回到首頁」→ C-00；再掃一次桌上 QR → C-01 顯示「整理中，請稍候」（權杖已失效，沒關掉的舊分頁再打 API 會拿到 `401 INVALID_SESSION_TOKEN`，一樣清掉權杖導到 C-00）

### 有做 A5 顧客線上結帳才加演（6.E7）

7. 手機按「我要結帳」→ C-09 帳單：品項明細、小計、服務費、合計、每人約多少，頂部提示「付款成功後這桌就不能再加點」
8. 同桌另一支手機加點 → C-10 按付款 → **回 C-09 並提示「剛剛有人加點，金額更新了，請再確認一次」**
9. 再付一次 → 選 LINE Pay → 2 秒動畫 → C-11 成功畫面 + 交易編號
10.（重複幾次可以演到失敗）失敗時顯示原因並可重試

### 有做 8.10 櫃檯入口才加演

11. 櫃檯結帳前在「會員（選填）」輸入手機 → 查詢 → 顯示「陳小美・1998/04/21」→ 按「確認是本人」→ 結帳 → 這次消費記到她名下
12. 輸入沒註冊的號碼 → 「查無此會員」；會員沒填生日 → 顯示「未提供」
