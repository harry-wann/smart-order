# 模組 1：菜單與品項

**難易度** ★★★☆☆　**里程碑** 第 1 週要能查、第 3 週後台能改

## 這個模組在做什麼

管理「店裡賣什麼」。分類、品項、價格、照片，還有每道菜可以怎麼客製（份量、辣度、加點）。

顧客端從這裡讀資料畫出菜單；店家端從這裡改價格、上下架、標售完。

**它是整個系統的地基**——沒有菜單，客人連東西都點不了。所以它是第 1 週第一個要打通的模組。

## 難易度為什麼是三顆星

CRUD 本身只有一顆星。難的是**選項群組模型**：

- 「份量」「辣度」「加點配料」看起來是三種不同的東西，其實是同一個機制
- 如果用「在 menu_item 上加欄位」的方式做，加到第三種就做不下去
- 多對多關聯（一個群組掛在多個品項上）對新手是個門檻

**做對了，之後所有客製化需求都不用改程式。做錯了，第 3 週要重寫。**

## 基礎必做功能

| # | 功能 | 驗收條件 | 狀態 |
|---|---|---|---|
| 1.1 | 分類 CRUD | 8 個分類可新增、改名、排序、停用 | ☐ |
| 1.2 | 品項 CRUD | 名稱、描述、價格、照片、所屬分類 | ☐ |
| 1.3 | 上下架 | 停用的品項顧客端完全看不到 | ☐ |
| 1.4 | 選項群組模型 | 一個群組可掛在多個品項上，改一次全部生效 | ☐ |
| 1.5 | 必選驗證 | `min_select=1` 的群組沒選，加入購物車按鈕停用 | ☐ |
| 1.6 | 鍋底標記 | `is_soup_base=true`；每桌至少一份，送出點餐時檢查（`400 SOUP_BASE_REQUIRED`） | ☐ |
| 1.7 | 鴛鴦鍋 | 用兩個 `min=1 max=1` 的群組（左鍋／右鍋）表達 | ☐ |
| 1.8 | 售完標示 | 庫存 0 時灰階 + 「售完」標籤 + 不可點 | ☐ |
| 1.9 | 免費續加品項 | `price=0`、`track_inventory=false`，但仍要出單 | ☐ |
| 1.10 | 飲食標示 | 素食／不含牛／辣度 0–3，顧客端顯示圖示 | ☐ |
| 1.11 | 菜單查詢 API | 依分類篩選、回傳含選項群組的品項詳情 | ☐ |
| 1.12 | 飲料與酒分類 | 就是一般分類，不做年齡驗證 | ☐ |

## 延伸加分功能

> **基礎全部綠燈後才動工。**

| # | 功能 | 說明 |
|---|---|---|
| 1.E1 | 套餐組合 | 一個品項包含多個子品項，需要新的資料表 |
| 1.E2 | 每日限量 | 每天重置的限量數，跟庫存不同 |
| 1.E3 | 午晚餐不同菜單 | 菜單依時段上下架，搭配排程 |
| 1.E4 | 圖片上傳 | 目前用外部圖片網址即可 |
| 1.E5 | 多語系 | |

## 會用到的技術

| 技術 | 為什麼需要 | 難度 |
|---|---|---|
| [ORM 與 JPA](../../tech/database/25-ORM與JPA.md) | Entity 對應資料表 | ★★★☆☆ |
| [JPA 關聯對應](../../tech/database/26-JPA關聯對應.md) | 品項↔選項群組的多對多 | ★★★★☆ |
| [資料表設計](../../tech/database/23-資料表設計.md) | 中間表、外鍵 | ★★★☆☆ |
| [Entity 與 DTO](../../tech/backend/17-Entity與DTO.md) | 回傳含選項群組的巢狀 DTO | ★★★☆☆ |
| [三層架構](../../tech/backend/16-三層架構.md) | Controller/Service/Repository | ★★☆☆☆ |
| [REST API 設計](../../tech/web/10-REST-API設計.md) | `/api/menu/items` | ★★☆☆☆ |
| [Flyway](../../tech/database/29-Flyway.md) | 建表 | ★★☆☆☆ |
| [Redis](../../tech/realtime/44-Redis.md) | 菜單快取（進階） | ★★★☆☆ |

---

## 問題思考與解決思路

### 問題 1：「份量」「辣度」「加點」要怎麼存？

**情境**
牛五花要能選半份／全份（價差 80）、原味／炙燒（價差 20）、還能加蔥花蒜泥（各 10 元，可複選）。

**直覺會這樣寫**
在 `menu_item` 上加欄位：

```sql
menu_item (
  has_size BOOLEAN, half_price DECIMAL, full_price DECIMAL,
  has_doneness BOOLEAN, ...
)
```

**為什麼會壞**
第三種需求出現時你會發現：欄位無限增加、每個品項的選項都不一樣、加一種新選項要改程式加欄位、而且「可複選」根本表達不出來。

**正確做法：選項群組模型**

```
option_group「份量」 min=1 max=1
  ├─ option_value「半份」 +0
  └─ option_value「全份」 +80

option_group「加點配料」 min=0 max=5
  ├─ 加蔥花 +10
  ├─ 加蒜泥 +10
  └─ 加香菜 +10

menu_item_option_group（中間表）
  menu_item_id ↔ option_group_id + sort_order
```

一個模型解決全部三種需求。`min_select` 和 `max_select` 兩個數字就決定了「必選／選填」「單選／複選」。

**延伸**
「辣度」這個群組建立一次，可以掛在 20 個品項上。改一次，20 個品項全部生效。

**面試會問**：「你怎麼設計商品規格？」這題答得好會加很多分。

---

### 問題 2：鴛鴦鍋要不要另外建表？

**情境**
共鍋店有單味鍋，也有鴛鴦鍋（一鍋兩味）。

**直覺會這樣寫**
建一張 `pot` 表，記錄「這桌有幾個鍋、每個鍋什麼湯底、鴛鴦的話兩個湯底各是什麼」。

**為什麼不好**
多一張表、多一套 CRUD、多一層關聯。而且「點菜要指定下哪一鍋」在真實店家根本不會發生——菜端上桌，客人自己決定下哪鍋。

**正確做法：用現有的選項群組**

```
menu_item「麻辣鍋底」      price 480, is_soup_base=true
menu_item「酸菜白肉鍋底」  price 420, is_soup_base=true
menu_item「昆布鍋底」      price 380, is_soup_base=true
menu_item「鴛鴦鍋底」      price 0,   is_soup_base=true
  ├─ option_group「左鍋湯底」 min=1 max=1 → 麻辣 +260 / 酸菜 +230 / 昆布 +210
  └─ option_group「右鍋湯底」 min=1 max=1 → 同上
```

**零額外資料表。** 鴛鴦價 = 左半 + 右半（各約單鍋的 55%），這也是真實店家的算法。想點兩鍋就點兩份。

---

### 問題 3：改了價格，三個月前的帳單會變嗎？

**情境**
老闆把牛五花從 280 調到 320。

**直覺會這樣寫**
`order_item` 只存 `menu_item_id`，要顯示時再去 join 菜單表拿名稱和價格。

**為什麼會壞**
**歷史帳單的金額會跟著變成 320。** 客人三個月前明明付 280，報表卻顯示 320。

**正確做法：存快照**

```sql
order_item (
  menu_item_id BIGINT,               -- 還是要，供統計與推薦用
  item_name_snapshot VARCHAR(100),   -- 下單當下的名稱
  unit_price_snapshot DECIMAL(10,2)  -- 下單當下的價格
)
```

這是少數「**故意重複存資料**」是正確的情況。

**延伸**
選項也要存快照（`order_item_option` 的 `group_name_snapshot` / `value_name_snapshot` / `price_delta`），否則刪掉一個選項後，歷史訂單會顯示空白。

---

### 問題 4：售完要怎麼讓所有人立刻看到？

**情境**
牛五花剛剛被最後一桌點完。此時有五桌客人正在看菜單。

**直覺會這樣寫**
菜單 API 每次都查一次資料庫，前端每 3 秒重新抓菜單。

**為什麼不好**
五桌 × 每 3 秒 = 每分鐘 100 次全菜單查詢，而且客人可能已經把它加進購物車了。

**正確做法：WebSocket 推播 + 送出時再驗一次**

1. 庫存歸零時，後端推 `MENU_SOLD_OUT` 到 `/topic/menu`
2. 所有正在點餐的裝置收到，把那個品項標成售完
3. **但這還不夠**——已經在購物車裡的怎麼辦？（購物車存在後端、整桌共用，重抓時每列會帶 `soldOut`，前端標紅）
4. 所以**送出點餐時後端一定要再檢查一次**，不夠就回 409

**前端的顯示是體驗，後端的檢查才是真相。**

---

## 六週切分

| 週 | 這個模組要交出什麼 |
|---|---|
| **W1** | Flyway 建好 `menu_category` / `menu_item` / `option_group` / `option_value` / 中間表；`GET /api/menu/categories` 和 `GET /api/menu/items` 能回傳種子資料；**顧客端 React 能把菜單列出來（walking skeleton）** |
| **W2** | `GET /api/menu/items/{id}` 回傳完整選項群組；必選驗證邏輯；顧客端品項詳情頁能選規格 |
| **W3** | 店家端菜單管理後台（品項 CRUD、上下架、快速下架開關）；選項群組管理 |
| **W4** | 飲食標示、免費續加品項 |
| **W5** | 售完狀態的 WebSocket 推播；菜單快取（選配） |
| **W6** | 不寫新功能。檢查售完顯示、圖片載入、RWD |
| **有餘力才做** | 人氣推薦查詢（進階 A7，M3 3.E1：`GROUP BY` + `SUM`，給 C-04 的「本店人氣 TOP 5」）。**基礎完成後才做** |

## 常見卡關

| 卡點 | 解法 |
|---|---|
| **多對多關聯寫不出來** | 不要用 `@ManyToMany`，把中間表做成 Entity（見 [JPA 關聯對應](../../tech/database/26-JPA關聯對應.md)） |
| **回傳品項詳情時噴 lazy 錯誤** | 在 Service 裡轉 DTO，或用 `JOIN FETCH`（見 [Entity 與 DTO](../../tech/backend/17-Entity與DTO.md)） |
| **選項價差算錯** | 用 `BigDecimal` 不要用 `double`；單價 = 基礎價 + 所有選項價差 |
| **種子資料太少，看不出效果** | 60 個品項、5 個選項群組是最低限度 |

## 驗收 Demo 腳本

1. 店家端改一個品項價格 → 顧客端重新整理 → 新價格生效
2. 店家端把一個品項下架 → 顧客端看不到了
3. 顧客端點「鴛鴦鍋底」→ 左右鍋各選一個湯底 → 價格正確累加
4. 點「牛五花」→ 不選份量 → 加入購物車按鈕是停用的
5. 一個庫存 0 的品項顯示灰階 + 售完標籤，且點不下去
