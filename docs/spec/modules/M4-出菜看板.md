# 模組 4：出菜看板（KDS）

**難易度** ★★★☆☆　**里程碑** 第 3 週

## 這個模組在做什麼

廚房牆上那塊螢幕。客人一送出點餐，這裡立刻跳出一張卡片，上面是桌號和要做的菜。廚師做好一樣就勾一樣，全部勾完卡片自動消失。

**KDS = Kitchen Display System**，取代傳統的出單機紙條。

## 難易度為什麼是三顆星

邏輯本身不難（就是查詢 + 更新狀態）。難的是三件事：

1. **即時性**——客人送出後廚房必須馬上看到，不能等
2. **狀態推導**——`order_ticket` 的狀態要由旗下 `order_item` 自動算出來，不能手動設
3. **可讀性設計**——廚房光線強、距離遠、手可能是濕的，UI 要跟一般後台完全不同

## 基礎必做功能

| # | 功能 | 驗收條件 | 狀態 |
|---|---|---|---|
| 4.1 | 看板查詢 | 以「點餐單」為單位的卡片，含桌號、單號、送出時間、品項 | ☐ |
| 4.2 | 即時推播 | 客人送出後 **1 秒內**卡片出現 | ☐ |
| 4.3 | 逐項勾選 | 每個品項可單獨標記已出餐，勾選框 ≥ 40×40px | ☐ |
| 4.4 | 整單完成 | 一鍵把整張單標記完成 | ☐ |
| 4.5 | 狀態自動推導 | 品項全部出完 → 單的狀態自動變 `SERVED` → 卡片淡出移除 | ☐ |
| 4.6 | 久候警示 | 等待超過 15 分鐘的單，邊框變紅 + 時間閃爍 | ☐ |
| 4.7 | 出餐順序建議 | 同一張單內，鍋底排最前、其次菜盤、最後肉品 | ☐ |
| 4.8 | 預約優先 | `is_preorder = true` 的單排最前面，金色邊框 + 「預約優先」標籤。**要有 M7 的預先點餐（7.E1～7.E3）才會有這種單** | ☐ |
| 4.9 | 退菜紀錄 | 做壞了可標記退菜並記原因，庫存不回補（已經用掉了） | ☐ |
| 4.10 | 深色 UI | 全站唯一使用深色背景的頁面 | ☐ |
| 4.11 | 大字級 | 桌號 ≥ 32px，品項 ≥ 20px | ☐ |
| 4.12 | 顧客端同步 | 勾選後，客人手機的「出餐進度」立刻更新 | ☐ |

## 延伸加分功能

| # | 功能 | 說明 |
|---|---|---|
| 4.E1 | 分區出單 | 冷盤區／熱食區／飲料吧各看各的 |
| 4.E2 | 製作時間統計 | 平均出餐時間、哪道菜最慢 |
| 4.E3 | 出單機列印佇列 | [RabbitMQ](../../tech/advanced/45-訊息佇列RabbitMQ.md) 的最佳場景 |
| 4.E4 | 叫號取餐 | 自助吧台用 |
| 4.E5 | 預估完成時間 | 依歷史資料推算 |

## 會用到的技術

| 技術 | 為什麼需要 | 難度 |
|---|---|---|
| [WebSocket 與 STOMP](../../tech/realtime/41-WebSocket與STOMP.md) | `/topic/kitchen` 頻道 | ★★★☆☆ |
| [前端接 WebSocket](../../tech/realtime/42-前端接WebSocket.md) | 訂閱與畫面更新 | ★★★☆☆ |
| [索引](../../tech/database/24-索引.md) | `(status, placed_at)` 讓看板查詢快 | ★★☆☆☆ |
| [JPA 關聯對應](../../tech/database/26-JPA關聯對應.md) | 一次抓出單 + 品項 + 選項 | ★★★★☆ |
| [Spring Security](../../tech/backend/20-SpringSecurity.md) | 只有 `KITCHEN` / `MANAGER` 能進 | ★★★★☆ |
| [Tailwind CSS](../../tech/frontend/39-Tailwind.md) | 深色主題與大字級 | ★★☆☆☆ |

---

## 問題思考與解決思路

### 問題 1：單的狀態要手動設還是自動算？

**情境**
一張單有五個品項。廚師勾了三個。這張單現在是什麼狀態？

**直覺會這樣寫**
每次勾選時，前端或後端手動判斷要不要把單改成 `PREPARING` 或 `SERVED`。

**為什麼會壞**
- 兩個廚師同時勾不同品項，狀態可能被覆蓋成錯的
- 取消一個品項時忘記重算
- 狀態和實際內容對不起來，而且很難 debug

**正確做法：由品項推導**

```java
public void refreshStatusFromItems() {
    List<ItemStatus> statuses = items.stream()
        .filter(i -> i.getStatus() != ItemStatus.CANCELLED)
        .map(OrderItem::getStatus).toList();

    if (statuses.isEmpty())                                   this.status = CANCELLED;
    else if (statuses.stream().allMatch(s -> s == SERVED))    this.status = SERVED;
    else if (statuses.stream().anyMatch(s -> s == SERVED))    this.status = PREPARING;
    else                                                      this.status = PENDING;
}
```

**每次品項狀態改變，就呼叫這個方法重算一次。** 單的狀態永遠等於「品項狀態的函數」，不可能對不起來。

**面試會問**：這是「單一真相來源（single source of truth）」的實際應用。

---

### 問題 2：預約先點的單怎麼排到最前面？

**情境**
一組客人線上訂位時就先點好菜並付了款，賣點是「到店後優先上菜」。

**做法 A：另外開一個「預約單」看板**
兩個看板，廚師要看兩邊。容易漏。

**做法 B：同一個看板，排序時排前面** ← 我們選這個

```sql
SELECT ... FROM order_ticket
WHERE status IN ('PENDING', 'PREPARING')
ORDER BY is_preorder DESC, placed_at ASC
```

**一個 `ORDER BY` 就解決了。** 再加上視覺區隔（金色邊框 + 標籤），廚師一眼就知道要先做哪張。

**關鍵細節**：`placed_at` 要用「**櫃檯開桌的時間**」而不是「客人線上點餐的時間」。否則一張三天前點的預約單，`placed_at` 是三天前，會被算成「等待了 4320 分鐘」，警示一直閃。

---

### 問題 3：廚房的 UI 為什麼要跟別的頁面不一樣

**情境**
KDS 螢幕掛在廚房牆上，廚師站在兩公尺外，手上可能是濕的。

**如果照一般後台的設計**
14px 的字、灰白背景、小小的勾選框——**廚師根本看不清楚，也點不準。**

**正確做法：為場景設計**

| 一般後台 | KDS |
|---|---|
| 亮色背景 `#FBF7F0` | **深色背景 `#241C18`** |
| 內文 14–16px | **品項 20px、桌號 32px** |
| 勾選框 16px | **勾選框 40×40px** |
| 資訊密度高 | 一排 4 張卡，留白多 |
| 滑鼠操作 | **觸控操作** |

**為什麼深色？** 廚房光線強、油煙多，深底白字的對比在遠距離最清楚，而且螢幕反光比較不影響。

**這是全站唯一使用深色的頁面。** 在 [11-設計系統](../../ui/11-設計系統.md) 有明確規範。

---

### 問題 4：卡片消失得太快，廚師來不及確認

**情境**
勾完最後一個品項，卡片立刻消失。廚師心想「我剛剛勾的是哪一桌？」

**正確做法：0.5 秒的淡出動畫 + 可以看已完成的單**

```jsx
// 先標記，動畫跑完再從清單移除
const handleComplete = (ticketId) => {
  setFading(prev => [...prev, ticketId]);
  setTimeout(() => {
    setTickets(prev => prev.filter(t => t.ticketId !== ticketId));
    setFading(prev => prev.filter(id => id !== ticketId));
  }, 500);
};
```

再加一個「最近完成」的分頁，可以看剛剛出完的 10 張單。**成本很低，但廚師用起來安心很多。**

---

## 六週切分

| 週 | 這個模組要交出什麼 |
|---|---|
| **W1** | （無）等 M3 的資料表建好 |
| **W2** | `GET /api/admin/kitchen/tickets` 查詢 API；`PATCH /api/admin/order-items/{id}/serve` |
| **W3** | ★ KDS 看板畫面（深色、大字、勾選框）；**WebSocket 即時推播**；狀態自動推導；**主流程全綠燈** |
| **W4** | 久候警示；預約優先排序；出餐順序建議 |
| **W5** | 退菜紀錄；最近完成分頁；平板實機測試 |
| **W6** | 不寫新功能。在真的平板上測一次，確認兩公尺外看得清楚 |

## 常見卡關

| 卡點 | 解法 |
|---|---|
| **查詢 N+1** | 一張單要抓品項、選項，用 `JOIN FETCH` 或 `@EntityGraph` |
| **WebSocket 推了但看板沒動** | 檢查訂閱的 topic 名稱有沒有打錯 |
| **狀態改了但顧客端沒同步** | 勾選後要同時推 `/topic/kitchen` 和 `/topic/session/{id}` |
| **等待時間顯示錯誤** | `placed_at` 用錯了（預約單的問題，見上面） |
| **平板上排版跑掉** | KDS 只做 ≥1024px，不用管手機 |

## 驗收 Demo 腳本

1. KDS 看板開在平板上（或另一個瀏覽器視窗）
2. 手機送出一張單 → **看板 1 秒內跳出卡片**
3. 卡片上桌號很大、品項清楚、選項有顯示（「全份・加蔥花」）
4. 勾選第一個品項 → **手機端的出餐進度立刻變成「已出餐」**
5. 勾完全部 → 卡片淡出消失
6. 一張預約先點的單 → **排在最前面，金色邊框 + 「預約優先」**
7. 一張放了 16 分鐘的單 → 紅色邊框警示
