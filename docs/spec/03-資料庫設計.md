# 03 資料庫設計

> MySQL 8.0 ／ `utf8mb4` ／ `utf8mb4_unicode_ci`
> ER 圖見 [06-圖表總覽](06-圖表總覽.md)。本文件是欄位層級的定義，建表以此為準。
> **所有建表與改表都透過 [Flyway](../tech/database/29-Flyway.md)**，`ddl-auto` 設為 `validate`。

## 0. 通用約定

| 約定 | 內容 |
|---|---|
| 主鍵 | 一律 `id BIGINT UNSIGNED AUTO_INCREMENT` |
| 命名 | 資料表單數 `snake_case`（`menu_item`），Java 端 `MenuItem` |
| 時間 | `created_at` / `updated_at` 全表皆有，`DATETIME(3)` |
| **金額** | **`DECIMAL(10,2)`，絕對不要用 `FLOAT`/`DOUBLE`** |
| 布林 | `TINYINT(1)` |
| 列舉 | Java 用 `enum` + `@Enumerated(STRING)`；DB 用 `VARCHAR(20)` |
| 軟刪除 | 只有 `menu_item`、`menu_category`、`dining_table` 用 `active` 旗標 |
| **快照** | 訂單相關的名稱與價格**必須存快照**，否則改菜單會讓歷史帳單金額跟著變 |

### Flyway 檔案規劃

```
db/migration/
├── V1__create_account_tables.sql       member, verification_code, staff
├── V2__create_menu_tables.sql          menu_category, menu_item, option_group, option_value, menu_item_option_group
├── V3__create_dining_tables.sql        dining_table, dining_session, dining_session_guest, cart_item, cart_item_option,
│                                       order_ticket, order_item, order_item_option
├── V4__create_inventory_tables.sql     inventory_item, inventory_log
├── V5__create_payment_tables.sql       payment
├── V6__create_reservation_tables.sql   reservation, reservation_preorder_item, reservation_preorder_option
├── V7__create_service_and_waitlist.sql service_call, waitlist
└── V8__create_member_benefit_tables.sql point_transaction, coupon, member_coupon   （進階 A6）
```

**migration 檔案要指定唯一負責人**（工作分配定案後指派），其他人要改表就開 issue 請他加。
**已經合併進 `develop` 的檔案永遠不能修改**——別人的資料庫已經跑過那一版了，改了 Flyway 會拒絕啟動。要改就寫一支新的。

---

## 1. 帳號與權限

### `member` 會員

| 欄位 | 型別 | 約束 | 說明 |
|---|---|---|---|
| id | BIGINT | PK | |
| phone | VARCHAR(20) | **UNIQUE, NOT NULL** | 登入帳號 `09xxxxxxxx` |
| nickname | VARCHAR(50) | NULL | C-13 補資料可跳過；**匿名轉會員（進階 8.10）時也可為空**，之後補 |
| gender | VARCHAR(10) | NULL | `MALE`/`FEMALE`/`OTHER`，供屬性推薦 |
| birth_date | DATE | NULL | 供年齡層推薦、生日優惠（8.E6）；櫃檯確認會員本人時會顯示給店員看（8.10，只顯示、不寫進任何紀錄） |
| points | INT | DEFAULT 0 | 點數餘額（快取欄位），**進階 A6 才用** |
| profile_completed | TINYINT(1) | DEFAULT 0 | 是否已補完資料 |
| status | VARCHAR(20) | | `ACTIVE`/`SUSPENDED` |
| created_at, updated_at | DATETIME(3) | | |

索引：`UNIQUE(phone)`

> **`nickname` 可為 NULL 是刻意的**：C-13 補資料的三個欄位都是選填，客人可以只留手機；
> 進階的匿名轉會員（8.10）在結帳頁登入就綁定，這時也只有手機號碼。等他真的想看紀錄時再補資料。

### `verification_code` 手機驗證碼

| 欄位 | 型別 | 說明 |
|---|---|---|
| id | BIGINT PK | |
| phone | VARCHAR(20) NOT NULL | |
| code | VARCHAR(6) NOT NULL | |
| attempt_count | INT DEFAULT 0 | 滿 3 次作廢 |
| expires_at | DATETIME(3) | 建立 +5 分鐘 |
| used_at | DATETIME(3) NULL | |
| created_at | DATETIME(3) | |

索引：`INDEX(phone, created_at)`

> **建議改用 [Redis](../tech/realtime/44-Redis.md)**（`SET otp:<phone> <code> EX 300`），天生有 TTL，不用寫清理排程。這張表是還沒導入 Redis 時的後備方案。

### `staff` 員工

| 欄位 | 型別 | 說明 |
|---|---|---|
| id | BIGINT PK | |
| username | VARCHAR(50) **UNIQUE** | |
| password_hash | VARCHAR(100) | BCrypt |
| display_name | VARCHAR(50) | |
| role | VARCHAR(20) | `COUNTER`/`KITCHEN`/`MANAGER` |
| active | TINYINT(1) DEFAULT 1 | |

> 種子資料建三個帳號：`counter/1234`、`kitchen/1234`、`manager/1234`，**並寫進 README**，面試官才能登入看。

---

## 2. 菜單

### `menu_category` 分類

id、name、sort_order、active

種子：鍋底／肉品／海鮮／蔬菜／火鍋料／主食／飲料／酒 共 8 類。

### `menu_item` 品項

| 欄位 | 型別 | 說明 |
|---|---|---|
| id | BIGINT PK | |
| category_id | BIGINT FK | |
| name | VARCHAR(100) | |
| description | VARCHAR(255) | |
| price | DECIMAL(10,2) | 基礎價 |
| image_url | VARCHAR(500) | |
| **is_soup_base** | TINYINT(1) DEFAULT 0 | 是否為鍋底；**鍋底每桌至少一份**（共鍋，非每人一份），送出點餐時檢查（04 §4.5，`400 SOUP_BASE_REQUIRED`） |
| **track_inventory** | TINYINT(1) DEFAULT 1 | **false = 免費續加品，不扣庫存** |
| **is_refill** | TINYINT(1) DEFAULT 0 | 菜單上歸類到「免費續加」區塊 |
| suitable_for | VARCHAR(20) | `ALL`/`ADULT`/`CHILD` |
| spicy_level | TINYINT | 0–3 |
| is_vegetarian | TINYINT(1) DEFAULT 0 | 素食標示 |
| contains_beef | TINYINT(1) DEFAULT 0 | 不吃牛的客人用 |
| sold_out | TINYINT(1) DEFAULT 0 | 由庫存自動維護 |
| sort_order | INT | |
| active | TINYINT(1) DEFAULT 1 | 下架用 |

索引：`INDEX(category_id, active, sort_order)`

**鍋底的種子資料（共鍋 + 鴛鴦）**

```
麻辣鍋底      price 480  is_soup_base=1
酸菜白肉鍋底  price 420  is_soup_base=1
昆布鍋底      price 380  is_soup_base=1
鴛鴦鍋底      price 0    is_soup_base=1
  掛兩個群組：「左鍋湯底」「右鍋湯底」，各 min=1 max=1
  選項價差：麻辣 +260 / 酸菜 +230 / 昆布 +210（約單鍋的 55%）
```

> **鴛鴦不需要額外資料表。** 「一桌幾個鍋」也不建模——想點兩鍋就點兩份。
>
> **「每桌至少一份」只看 `is_soup_base`。** 舊版另有一個 `is_required_on_open` 欄位，是「開桌先選鍋底」（舊 C-03）留下的名字；
> 現在規則改在送出點餐時擋，四種鍋底兩個旗標值又都一樣，所以刪掉，不要再加回來。

### `option_group` 選項群組

| 欄位 | 說明 |
|---|---|
| id, name | 「份量」「辣度」「左鍋湯底」「加點配料」 |
| min_select | 0 = 非必選 |
| max_select | 1 = 單選；>1 = 可複選 |
| sort_order | |

**群組不綁死在單一品項上**，透過下面的關聯表共用。「辣度」建立一次可掛在 20 個品項上。

### `menu_item_option_group` 品項 ↔ 群組（多對多）

`menu_item_id`、`option_group_id`、`sort_order`
主鍵：`PRIMARY KEY(menu_item_id, option_group_id)`

> **不要用 `@ManyToMany`**，因為中間表有 `sort_order`。做成獨立 Entity，見 [JPA 關聯對應](../tech/database/26-JPA關聯對應.md)。

### `option_value` 選項

id、option_group_id FK、name、`price_delta DECIMAL(10,2)`、sort_order、active

---

## 3. 桌位、用餐與點餐

### `dining_table` 桌位

| 欄位 | 型別 | 說明 |
|---|---|---|
| id | BIGINT PK | |
| table_no | VARCHAR(10) **UNIQUE** | A01、A02、B01… |
| seats | INT | 2／4／6／8 |
| area | VARCHAR(20) | 「大廳」「包廂」 |
| status | VARCHAR(20) | `AVAILABLE`/`OCCUPIED`/`CLEANING` |
| **reservable** | TINYINT(1) DEFAULT 1 | 開放線上訂位（保留 30% 給現場） |
| active | TINYINT(1) DEFAULT 1 | |

種子：12 張桌（2 人 ×2、4 人 ×6、6 人 ×2、8 人 ×2），其中 8 張 `reservable = 1`。

> **桌位狀態只有三種，而且只反映現場當下。** 這個欄位**不存**「預約保留」。
>
> **預約保留（M7 7.10）是查詢時算出來的顯示狀態。** `AVAILABLE` 的桌如果有一筆訂位同時符合
> 配到這張桌、`CONFIRMED`、現在落在 `start_time − 30 分` ～ `start_time + 10 分`，
> 桌況 API 就回 `displayStatus = RESERVED`。所以訂位會在時間窗內鎖住配到的桌；**候位不鎖桌**。
> 要解開預約保留，就是取消那筆訂位（`status` → `CANCELLED`、`table_id` 設 NULL），條件自然不成立，桌位欄位什麼都不用改。
> 另一個接點照舊：**開桌成功時，把來源那筆訂位或候位轉成已報到**。
>
> 舊版也有一個 `RESERVED`，但它**存在這個欄位裡**，由排程在訂位前 30 分鐘自動轉入——那等於把 `reservation`
> 複製一份到桌位上、還要靠 cron 同步，一旦不同步就會出現
> 「畫面顯示空桌、按開桌卻被擋」這種最難查的狀況。那個做法已經移除；
> 現在每次查詢現算，不存、不靠排程，就沒有同步這件事（見 [M2 問題 5](modules/M2-桌位開桌與候位.md)）。

```sql
-- 這張桌現在被哪筆訂位保留（查不到就是沒保留）
-- 「現在落在 start − 30 ～ start + 10」換成查 start_time，就是「start_time 落在 now − 10 ～ now + 30」
SELECT * FROM reservation
 WHERE table_id = :tableId
   AND status = 'CONFIRMED'
   AND start_time BETWEEN :now - INTERVAL 10 MINUTE AND :now + INTERVAL 30 MINUTE
 ORDER BY start_time
 LIMIT 1;
```

30、10 兩個數字從 `application.yml` 讀（`reservation.hold-before-minutes`、`reservation.hold-after-minutes`），上面寫死只是好讀。
`idx_reservation_table_time (table_id, start_time, end_time)` 可以直接用上（先比 `table_id`，再掃 `start_time` 的範圍）。

> **只有訂位會鎖桌，候位不鎖桌。** 桌子會被鎖只有兩種來源：預約保留（上面算出來的）與用餐中／待清理（這個欄位）。
> 候位叫號只是通知，客人到櫃檯才選桌開桌，所以 `dining_table` **沒有**任何「保留給誰」的欄位，`status` 也只有三種。
> （原本規劃過一個「櫃檯自己把空桌鎖起來」的延伸功能與對應的狀態、欄位，已經刪除，見 [M2 問題 5](modules/M2-桌位開桌與候位.md)。）

### `dining_session` 用餐紀錄（＝帳單）★核心

| 欄位 | 型別 | 說明 |
|---|---|---|
| id | BIGINT PK | |
| table_id | BIGINT FK | |
| member_id | BIGINT FK **NULL** | 匿名點餐時為 NULL；綁會員（進階 8.10）有兩個入口：C-09 線上登入、S-03 櫃檯結清時帶 `memberId`（同一個交易寫入） |
| reservation_id | BIGINT FK NULL | 由訂位而來時填 |
| waitlist_id | BIGINT FK NULL | 由候位而來時填 |
| session_token | VARCHAR(64) **UNIQUE** | 顧客端 API 憑證 |
| adult_count / child_count | INT | **不參與金額計算**（見下方說明） |
| status | VARCHAR(20) | `OPEN`/`PAID`/`CANCELLED`（**沒有**「結帳中」，見下方說明） |
| subtotal | DECIMAL(10,2) | 品項小計 |
| service_fee | DECIMAL(10,2) | 小計 × 10% |
| discount | DECIMAL(10,2) DEFAULT 0 | 優惠券＋點數折抵合計（可以同時用），**進階 A6 才用**，基礎永遠是 0 |
| total | DECIMAL(10,2) | 合計 |
| cart_version | BIGINT NOT NULL DEFAULT 0 | **購物車版本號**。購物車每次異動（加／改／刪／送出後清空）就 +1，跟著 `CART_UPDATED` 推給同桌，前端用它擋掉亂序或過期的推播（見 [04 §3.4](04-API規格.md)） |
| opened_at / closed_at | DATETIME(3) | |

索引：`INDEX(table_id, status)`、`INDEX(member_id, opened_at)`、`UNIQUE(session_token)`、`INDEX(reservation_id)`（外鍵本來就會自動建；訂位的區間重疊要靠它找「這筆報到的用餐紀錄結帳了沒」）

> **`cart_version` 為什麼不用時間戳？**
> 購物車是 `cart_item` 的**多列集合**，不是單一列，所以沒有一個現成的「整份購物車的時間」可用。
> 拿 `MAX(updated_at)` 會在**刪除**時出錯——被刪的那列消失了，`MAX` 可能反而變小，
> 於是「刪除」這個事件看起來比它前一個事件還舊，會被前端當成過期丟掉。
> `DATETIME(3)` 只到毫秒也會碰撞：同桌兩支手機同一毫秒各加一樣東西，兩個事件的時間戳相同，分不出先後。
> 單調遞增的版本號兩個問題都沒有，而且就是 JPA 樂觀鎖的 `@Version`（見 [鎖與併發](../tech/database/28-鎖與併發.md)），Hibernate 可以自動維護。

> **人數為什麼不參與金額計算？**
> 我們的計價是「一鍋一價 + 單點」、無低消。人數的用途是：座位安排、分帳均分的參考值、兒童友善推薦、客單價統計。
> **這點一定要寫清楚**，不然會有人自己加一個人頭費進去。

> **一張桌同時只能有一筆 `status = OPEN` 的 session。** MySQL 沒有部分唯一索引，這條規則在 Service 層用 `SELECT ... FOR UPDATE` 保證（見 [M2 問題 2](modules/M2-桌位開桌與候位.md)）。

> **為什麼沒有 `CHECKOUT_PENDING`？** 舊版要求結帳時會把 session 鎖成「結帳中」、不能加點。
> 現在基礎是客人到櫃檯結帳，改成**金額比對 + 列鎖**：送出點餐、櫃檯結清、（進階）線上付款
> 三個地方都在交易裡 `SELECT ... FOR UPDATE` 這一列；結清／付款帶 `expectedTotal`，
> 重算不相等回 `409 BILL_CHANGED`，已經不是 `OPEN` 回 `409 SESSION_CLOSED`。
> 所以這張表不需要額外的鎖定欄位，詳見 [M6 問題 1](modules/M6-結帳.md)。

### `dining_session_guest` 同桌客人（這桌的「誰」）★2026-09-16 新增

| 欄位 | 型別 | 說明 |
|---|---|---|
| id | BIGINT PK | |
| dining_session_id | BIGINT FK | |
| device_id | VARCHAR(36) | 前端第一次開啟時產生的 UUID，存 localStorage，請求時用 `X-Device-Id` 帶上來 |
| display_name | VARCHAR(50) | 有登入會員、而且有填暱稱就用暱稱，否則依加入順序給「客人 1」「客人 2」… |
| member_id | BIGINT FK NULL | 加入時有帶會員 JWT 才填 |
| joined_at | DATETIME(3) | |

索引：`UNIQUE(dining_session_id, device_id)`（同一支手機重複加入拿到同一筆）

> **為什麼要這張表？** 同一桌只有一個 `session_token`，分不出是哪支手機在操作；
> 共用購物車要顯示「陳小美 加的」，就得另外記「誰」。
> **`device_id` 只拿來標示誰加的，不是身分驗證**——驗證仍然靠 `session_token`；被人偽造頂多顯示錯名字。

### `cart_item` 共用購物車 ★2026-09-16 新增

| 欄位 | 型別 | 說明 |
|---|---|---|
| id | BIGINT PK | |
| dining_session_id | BIGINT FK | **整桌共用一份**，不分手機 |
| menu_item_id | BIGINT FK | |
| quantity | INT | |
| note | VARCHAR(50) NULL | |
| unit_price | DECIMAL(10,2) | 加入當下的單價（基礎價＋選項價差），**只給畫面顯示**；送單時仍以菜單現價重算 |
| added_by_guest_id | BIGINT FK | → `dining_session_guest`，畫面上的「陳小美 加的」 |
| created_at, updated_at | DATETIME(3) | |

索引：`INDEX(dining_session_id)`

### `cart_item_option` 購物車選項

`cart_item_id` FK、`option_value_id` FK；主鍵 `PRIMARY KEY(cart_item_id, option_value_id)`

> **購物車階段不快照**，只記選了哪個 `option_value_id`；送單時才讀當下的名稱與價差，寫進 `order_item_option`。

> **為什麼存 DB，不放前端、HTTP session 或 Redis？**
>
> | 放哪裡 | 問題 |
> |---|---|
> | 前端 state／localStorage | 每支手機各一份，同桌看不到彼此加了什麼 |
> | HTTP session | 一樣是每支手機各一份，伺服器重啟也會消失 |
> | Redis | 送出時要「寫 `order_ticket`＋刪購物車」一起成功或一起失敗，跨 Redis 和 MySQL 做不到同一個交易 |
>
> 送出點餐時：鎖 `dining_session` 那列 → 讀整桌 `cart_item` → 驗證、扣庫存、快照 → 寫 `order_ticket`／`order_item` → 刪掉這些 `cart_item`，
> 全部在同一個交易裡（見 [04 §4.5](04-API規格.md)）。
> session 進終態（`PAID` 或 `CANCELLED`）時，整桌購物車在同一個交易裡刪掉——還沒送出的東西直接捨棄，不算錢。
> 櫃檯代客加點（`source = STAFF`）與預先點餐（`reservation_preorder_item`）都**不經過**這張表。

### `order_ticket` 點餐單 ★核心

| 欄位 | 型別 | 說明 |
|---|---|---|
| id | BIGINT PK | |
| dining_session_id | BIGINT FK | |
| sequence_no | INT | 該次用餐的第幾單 |
| status | VARCHAR(20) | `PENDING`/`PREPARING`/`SERVED`/`CANCELLED` |
| **is_preorder** | TINYINT(1) DEFAULT 0 | 訂位時先點的單，KDS 排最前 |
| source | VARCHAR(20) | `CUSTOMER`／`STAFF`（櫃檯代點） |
| submitted_by_guest_id | BIGINT FK NULL | 誰按的「送出整桌點餐」（→ `dining_session_guest`），同桌通知「王大明 送出 6 項」用；櫃檯代點與預點轉單為 NULL |
| placed_at | DATETIME(3) | **預約單用「開桌時間」而非「線上點餐時間」** |
| cancel_reason | VARCHAR(100) NULL | |
| updated_at | DATETIME(3) | |

索引：`INDEX(dining_session_id, sequence_no)`、`INDEX(status, placed_at)`

### `order_item` 單品 ★核心

| 欄位 | 型別 | 說明 |
|---|---|---|
| id | BIGINT PK | |
| order_ticket_id | BIGINT FK | |
| menu_item_id | BIGINT FK | 供統計與推薦 |
| **item_name_snapshot** | VARCHAR(100) | 下單當下的名稱 |
| **unit_price_snapshot** | DECIMAL(10,2) | 下單當下的基礎價 |
| options_price | DECIMAL(10,2) DEFAULT 0 | 所有選項價差合計 |
| quantity | INT | |
| line_total | DECIMAL(10,2) | `(unit_price + options_price) × quantity` |
| note | VARCHAR(50) NULL | |
| status | VARCHAR(20) | `PENDING`/`SERVED`/`CANCELLED` |
| cancel_reason | VARCHAR(100) NULL | 退菜原因 |
| served_at | DATETIME(3) NULL | |

索引：`INDEX(order_ticket_id)`、**`INDEX(menu_item_id)`**（推薦查詢會大量用到）

### `order_item_option` 單品選項快照

id、order_item_id FK、`group_name_snapshot`、`value_name_snapshot`、`price_delta`

> 選項也要存快照，否則刪掉一個選項後歷史訂單會顯示空白。

### `service_call` 服務鈴 ★新增

| 欄位 | 型別 | 說明 |
|---|---|---|
| id | BIGINT PK | |
| dining_session_id | BIGINT FK | |
| table_id | BIGINT FK | 冗餘存一份，櫃檯查詢方便 |
| type | VARCHAR(20) | `SERVICE`（呼叫服務生）／`SOUP`（加湯）／`POT_CLEAN`（換鍋撈渣）。**沒有結帳**，客人要結帳請到櫃檯 |
| status | VARCHAR(20) | `PENDING`/`ACKNOWLEDGED`/`DONE` |
| created_at | DATETIME(3) | |
| acknowledged_at | DATETIME(3) NULL | |
| staff_id | BIGINT FK NULL | 誰處理的 |

索引：`INDEX(status, created_at)`

> **為什麼要存表而不是只推 WebSocket？** 櫃檯重新整理後還看得到未處理的、可以統計、可以標記已處理。

---

## 4. 候位 ★新增

### `waitlist`

| 欄位 | 型別 | 說明 |
|---|---|---|
| id | BIGINT PK | |
| ticket_no | VARCHAR(10) | A012、B003（依人數分組） |
| contact_name | VARCHAR(50) | |
| contact_phone | VARCHAR(20) | 末四碼即可 |
| adult_count / child_count | INT | |
| party_size | INT | 總人數，供配桌用 |
| status | VARCHAR(20) | `WAITING`/`CALLED`/`SEATED`/`ABANDONED`/`EXPIRED` |
| created_at | DATETIME(3) | |
| called_at | DATETIME(3) NULL | **排程從這裡算 10 分鐘過號** |
| seated_at | DATETIME(3) NULL | |
| dining_session_id | BIGINT FK NULL | 報到後綁定 |

索引：`INDEX(status, created_at)`、`INDEX(called_at)`

---

## 5. 訂位

### `reservation`

| 欄位 | 型別 | 說明 |
|---|---|---|
| id | BIGINT PK | |
| **reservation_no** | VARCHAR(20) **UNIQUE** | 預約編號，格式 `R-YYMMDD-流水號`（用餐日期＋`id` 補零到 4 位，例 `R-260920-0442`）；insert 拿到 `id` 後在同一個交易裡填上。C-18 大字顯示、客人報到或來電查詢用 |
| member_id | BIGINT FK NULL | 非會員也能訂 |
| contact_name | VARCHAR(50) | |
| contact_phone | VARCHAR(20) | |
| contact_email | VARCHAR(100) NULL | 匿名訂位選填（C-16b）；**只存不寄**，寄確認信是延伸（見 M7） |
| **start_time** | DATETIME(3) NOT NULL | 用餐開始 |
| **end_time** | DATETIME(3) NOT NULL | **= start + 100 分 + 15 分緩衝** |
| adult_count / child_count | INT | |
| party_size | INT | |
| table_id | BIGINT FK **NULL** | **訂位當下 best-fit 配到的桌**；取消時設為 NULL 以釋放唯一約束 |
| status | VARCHAR(20) | `CONFIRMED`/`SEATED`/`CANCELLED`/`NO_SHOW` |
| **access_token** | VARCHAR(64) **UNIQUE** | 匿名訂位用來回來查詢與改單的憑證 |
| preorder_updated_at | DATETIME(3) NULL | 最後一次預點異動時間；`NULL` = 沒預點（進階） |
| created_at | DATETIME(3) | |
| **cancelled_at** | DATETIME(3) NULL | 取消的時間（客人線上取消或櫃檯代客取消）；沒取消是 NULL |
| **cancelled_by_staff_id** | BIGINT FK NULL | → `staff`，櫃檯代客取消時是哪個員工；客人自己取消時為 NULL |
| **cancel_source** | VARCHAR(10) NULL | `CUSTOMER`（客人線上取消，M7 7.8）/ `STAFF`（櫃檯代客取消，M7 7.18）；沒取消是 NULL。`STAFF` 時 `cancelled_by_staff_id` 一定有值 |

> **狀態只有四種，沒有 `PENDING`。** 舊流程是「先建訂位 → 預付成功才確認」，
> 所以需要一個中間狀態。現在**選完時段送出就直接是 `CONFIRMED`**，沒有任何路徑會產生 `PENDING`，
> 留著只會讓人猜。之後要做訂金（M7 的 7.E7）再加回來。

> **`access_token` 是給匿名客人的。** 會員靠 JWT 就能存取自己的訂位；
> 匿名沒有帳號，但一樣要能回來看訂位、改預點內容。作法跟 `dining_session.session_token`
> 完全一樣：建立時發一個隨機字串，前端存 localStorage。
> 沒有它的話，只要猜到 `reservation.id` 就能改別人的單。

> **預約保留不另外存狀態。** 保不保留由 `table_id`、`status`、`start_time` 當場算出來（見上面 `dining_table` 的說明）。
> 要解開保留就是取消訂位：`status` 改 `CANCELLED`、填 `cancelled_at`／`cancel_source`（櫃檯取消再加 `cancelled_by_staff_id`）、`table_id` 設 NULL。
> 沒有「訂位照留、只解除保留」的欄位。

> **取消有兩個入口，期限不同**：客人線上取消只到訂位時間前 30 分鐘（之後回 `409 CANCEL_DEADLINE_PASSED`）；
> 櫃檯代客取消隨時可以。兩者都只能取消 `CONFIRMED` 的訂位（其他回 `409 RESERVATION_NOT_CANCELLABLE`）。
> 櫃檯取消時 body 可帶的 `reason` 只寫日誌，**不存欄位**。

**關鍵索引**

```sql
-- 資料庫層的保險：同一桌、同一開始時間不能有兩筆
CREATE UNIQUE INDEX uk_reservation_table_start ON reservation (table_id, start_time);

-- 區間重疊檢查用
CREATE INDEX idx_reservation_table_time ON reservation (table_id, start_time, end_time);
CREATE INDEX idx_reservation_start ON reservation (start_time, status);
CREATE INDEX idx_reservation_phone ON reservation (contact_phone);
```

> **重疊判斷式**：`existing.start_time < :newEnd AND :newStart < existing.end_time`，而且只看 `CONFIRMED`，以及 `SEATED` 而且那筆用餐紀錄還沒結帳的：
> `status = 'CONFIRMED' OR (status = 'SEATED' AND EXISTS (SELECT 1 FROM dining_session ds WHERE ds.reservation_id = r.id AND ds.status = 'OPEN'))`
> ——客人還坐著就算佔用；結帳（`PAID`）或取消用餐之後名額立刻釋出，不用等到原本的 `end_time`。
> 線上訂位只能訂明天以後，所以這條主要影響的是「同一天之後的訂位保留」與容量報表的正確性，不會出現當天被線上搶訂的情況。
> **用 `<` 不是 `<=`**，因為「剛好接續」不算衝突。詳見 [區間重疊與訂位排程](../tech/advanced/46-區間重疊與訂位排程.md)。

> **取消時把 `table_id` 設為 NULL**，這樣唯一約束就不會擋住「取消後同一時段重新訂」。狀態紀錄仍然保留。

> **`reservation.table_id` 是「訂位當下配的桌」，不是「客人實際坐的桌」。**
> 它的用途有兩個：**容量與區間重疊判斷**——這個時段還有沒有位子可以再賣；
> 以及**訂位時間窗內保留那張桌**（7.10）。
>
> 客人到店後實際坐哪一張，由 `dining_session.table_id` 與 `dining_session.reservation_id`
> 這一組記錄。**開桌時不回頭改 `reservation.table_id`**，因為那會讓區間重疊的歷史資料
> 跟著現場調度一起變動，訂位容量就算不準了。
>
> 兩個欄位回答兩個不同的問題，不要想合併：
>
> | 問題 | 看哪裡 |
> |---|---|
> | 19:00 這個時段還能不能再接訂位 | `reservation.table_id` + `start_time` / `end_time` |
> | 18:30 的訂位現在該不該鎖住 A08 | `reservation.table_id` + `start_time` + `status` |
> | 陳小姐今天實際坐哪一桌、消費多少 | `dining_session`（它的 `reservation_id` 指回來） |

---

## 6. 付款

### `payment`

| 欄位 | 型別 | 說明 |
|---|---|---|
| id | BIGINT PK | |
| dining_session_id | BIGINT FK NULL | 用餐結帳（櫃檯結清或 A5 線上付款） |
| reservation_id | BIGINT FK NULL | 訂金（延伸 7.E7），**這一版不會用到** |
| method | VARCHAR(20) | 櫃檯結清：`CASH`/`CARD`/`BARCODE`（按下後倒數 5 秒的模擬流程）；進階 A5 線上付款：`APPLE_PAY`/`GOOGLE_PAY`/`LINE_PAY`。**沒有 `MOCK_PAY`** |
| amount | DECIMAL(10,2) | |
| status | VARCHAR(20) | `PENDING`/`SUCCESS`/`FAILED`/`REFUNDED` |
| transaction_no | VARCHAR(50) | `MOCK-20260913-000123` |
| fail_reason | VARCHAR(100) NULL | |
| paid_at / refunded_at | DATETIME(3) NULL | |

> `dining_session_id` 與 `reservation_id` 恰有一個不為 NULL，在 Service 層檢查。
> **所有付款都是模擬的，不串接任何真實金流 API。**
> 櫃檯結清（`POST /api/admin/dining-sessions/{id}/settle`）也寫一筆 `status = SUCCESS` 的紀錄，
> 報表才分得出現金、信用卡、條碼；櫃檯結清不做失敗機率，`PENDING`／`FAILED` 只有 A5 線上付款會出現。

> **訂位與預先點餐都不產生 payment。** 餐點金額一律在到店結帳時才算，
> 所以「調整預點」「取消訂位」沒有退款這回事。`reservation_id` 這欄先留著，
> 是給之後可能要做的訂金功能用的。

### `reservation_preorder_item` 預先點餐品項（進階）

| 欄位 | 型別 | 說明 |
|---|---|---|
| id | BIGINT PK | |
| reservation_id | BIGINT FK | |
| menu_item_id | BIGINT FK | |
| **item_name_snapshot** | VARCHAR(100) | 預點當下的名稱 |
| **unit_price_snapshot** | DECIMAL(10,2) | 預點當下的基礎價，**報到轉單時照抄，不重新取價** |
| options_price | DECIMAL(10,2) DEFAULT 0 | 選項價差合計 |
| quantity | INT | |
| line_total | DECIMAL(10,2) | `(unit_price + options_price) × quantity` |
| note | VARCHAR(50) NULL | |

索引：`INDEX(reservation_id)`

### `reservation_preorder_option` 預點選項快照（進階）

id、reservation_preorder_item_id FK、`group_name_snapshot`、`value_name_snapshot`、`price_delta`

> **為什麼不直接借用 `order_ticket` / `order_item`？**
> `order_ticket` 的語意是「送出即定案，進 KDS、扣庫存、不可改」，
> 但預點在報到前必須能反覆修改，兩個語意會打架；
> 而且訂位時還沒有桌、沒有 `dining_session`，硬要先開一筆會讓
> 「今天有幾桌在用餐」這種查詢全部要多過濾條件。
> 所以預點自己一組表，**報到開桌時逐筆複製成 `order_item`**（欄位幾乎一樣，轉換很單純）。
> 完整推導見 [M7 問題 6](modules/M7-訂位.md)。

> **預點不扣庫存**，庫存在報到轉單那一刻才扣。理由與取捨見 [M7 問題 7](modules/M7-訂位.md)。

---

## 7. 庫存

### `inventory_item`

| 欄位 | 型別 | 說明 |
|---|---|---|
| id | BIGINT PK | |
| menu_item_id | BIGINT FK **UNIQUE** | 品項層級庫存（1:1） |
| quantity | INT NOT NULL | **防超賣的關鍵欄位** |
| unit | VARCHAR(10) | 「份」「盤」「碗」 |
| low_threshold | INT | 低於此值警示 |
| updated_at | DATETIME(3) | |

**扣庫存的唯一正確寫法**

```sql
UPDATE inventory_item
   SET quantity = quantity - :qty, updated_at = NOW(3)
 WHERE menu_item_id = :itemId
   AND quantity >= :qty;
-- 影響筆數 0 → 庫存不足，整張點餐單回滾並回 409
```

> **不要先 `SELECT` 檢查再 `UPDATE`。** 兩個人同時點最後一份時，兩邊的 SELECT 都會通過。
> 這是整個專案最能展現「有想過並行問題」的地方，**每位成員都要會講**。詳見 [鎖與併發](../tech/database/28-鎖與併發.md)。

> **`track_inventory = false` 的品項（免費續加）跳過這段**，不扣也不會被標售完。

### `inventory_log`

id、inventory_item_id FK、`change_qty`（正負）、`quantity_after`、`reason`（`ORDER`/`RESTOCK`/`ADJUST`/`CANCEL`/`WASTE`）、`ref_id`、`operator`、`created_at`

> 「叫貨統計」（進階）＝ 這張表依 `reason='ORDER'` 依日期／品項 group by，不需要額外資料表。

---

## 8. 會員權益（進階 A6）

> 這三張表、`member.points` 與 `dining_session.discount` 都是 **A6 點數與優惠券**（C-20）才用。
> 基礎畫面上不出現點數與優惠券。A6 沒有店家端發券畫面，券先用種子資料建。

### `point_transaction`
id、member_id、dining_session_id NULL、`change_amount`（正負）、`balance_after`、`type`（`EARN`/`REDEEM`/`EXPIRE`/`ADJUST`）、created_at

> `member.points` 是快取欄位，真實來源是這張表的 `SUM(change_amount)`。
>
> `EARN`：以**實付金額**計，每滿 100 元 1 點、無條件捨去（1,562 元 → 15 點），付款成功後才寫。
> `REDEEM`：1 點折 1 元。優惠券與點數可以同一次一起用：先扣券（一次最多一張）→ 再扣點，最多折到 0 元；`dining_session.discount` 存兩者合計。

### `coupon` / `member_coupon`
`coupon`：code、name、type（`AMOUNT`/`PERCENT`）、value、min_spend、valid_from、valid_to、active
`member_coupon`：member_id、coupon_id、status（`UNUSED`/`USED`/`EXPIRED`）、used_at、dining_session_id

---

## 9. 索引總表

| 資料表 | 索引 | 為什麼 |
|---|---|---|
| member | `UNIQUE(phone)` | 登入查詢 |
| dining_table | `UNIQUE(table_no)` | |
| dining_session | `(table_id, status)` | 桌況頁高頻查詢 |
| dining_session | `UNIQUE(session_token)` | 每次顧客端請求都要驗 |
| dining_session | `(reservation_id)` | 區間重疊判斷：已報到的訂位，用餐紀錄還是 `OPEN` 才算佔用 |
| dining_session_guest | `UNIQUE(dining_session_id, device_id)` | 同一支手機重複加入拿到同一筆 |
| cart_item | `(dining_session_id)` | 撈整桌購物車 |
| order_ticket | `(dining_session_id, sequence_no)` | 本桌訂單 |
| order_ticket | `(status, placed_at)` | KDS 看板 |
| order_item | `(menu_item_id)` | **人氣推薦統計**（進階 A7） |
| service_call | `(status, created_at)` | 櫃檯未處理清單 |
| waitlist | `(status, created_at)`、`(called_at)` | 候位清單與過號排程 |
| reservation | `UNIQUE(table_id, start_time)` | **防重複訂位** |
| reservation | `(table_id, start_time, end_time)` | **區間重疊檢查**、預約保留判斷 |
| reservation | `(start_time, status)` | 排程掃描 |
| reservation | `UNIQUE(access_token)` | 匿名訂位的存取憑證 |
| reservation_preorder_item | `(reservation_id)` | 撈整份預點內容 |
| inventory_item | `UNIQUE(menu_item_id)` | |

---

## 10. 種子資料策略 ★第 1 週必做

> **最容易被忽略、但最容易在 demo 當天翻車的一件事。** 推薦、報表、備貨預測在空資料庫上全是空白。

| 資料 | 數量 | 備註 |
|---|---|---|
| 分類 | 8 | 鍋底／肉品／海鮮／蔬菜／火鍋料／主食／飲料／酒 |
| 品項 | 約 60 | 含 4 種鍋底（3 單味 + 1 鴛鴦）、3～4 個免費續加品 |
| 選項群組 | 6～7 組 | 份量、辣度、熟度、加點配料、左鍋湯底、右鍋湯底、冰塊 |
| 桌位 | 12 張 | 2×2、4×6、6×2、8×2；其中 8 張 `reservable=1` |
| 員工 | 3 | counter / kitchen / manager |
| 會員 | 40 | **性別各半、年齡分佈 20～65 歲**（否則屬性推薦看不出效果） |
| 歷史用餐 | 約 400 場（過去 90 天） | 平日／假日量要有差異 |
| 歷史點餐單 | 約 1200 張 | 平均每場 3 張 |
| 歷史單品 | 約 5000 筆 | **要有明顯的熱門／冷門差異** |
| 庫存 | 每品項 20～100 | **刻意留 2～3 個品項庫存 = 0** 用來 demo 售完 |
| 未來訂位 | 未來 7 天約 20 筆 | 另放今天幾筆（線上訂位只能訂明天以後，今天的這幾筆由種子直接寫進資料表，不走 `POST /api/reservations`）：三筆 30 分鐘內要到、配到不同桌（開桌頁看得到，配到的桌顯示「預約保留」；M7 demo 報到、原保留桌、櫃檯代客取消各用一筆；代客取消那筆也用來 demo 客人線上取消被 `CANCEL_DEADLINE_PASSED` 擋下）、一筆約 1 小時後（demo「18:00 起可報到」與 `RESERVATION_TOO_EARLY`；它配到的桌也用來 demo 開桌強烈提醒，因為落在 115 分鐘內）、一筆已過訂位時間 10 分鐘以上（demo 逾時與 `RESERVATION_EXPIRED`） |
| 候位 | 3～5 筆等待中 | |

### 實作建議

- 表結構用 [Flyway](../tech/database/29-Flyway.md)（`V` 開頭）
- **種子資料用程式產生**（`CommandLineRunner` + `@Profile("local")`），不要手寫 SQL——5000 筆手寫不完
- 亂數要有規律：週五六人多、**每桌必定至少一份鍋底**、肉品機率高於甜點
- 保留一組「乾淨的 demo 桌」（例如 A01）

### Demo 前的重置腳本

準備 `reset-demo.sh`：清掉當日的 session 與 order，保留歷史資料與菜單。**Demo 前 10 分鐘跑一次。** 這個腳本救過很多組。
