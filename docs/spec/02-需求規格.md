# 02 需求規格：基礎與進階功能總表

> 分層原則見 [00-架構分層與技術選型](00-架構分層與技術選型.md)。
> 每個模組的詳細規格、問題思考與六週切分，見 [modules/](modules/) 底下各頁。

## 1. 分層判準

- **基礎（核心細項）** = 拿掉它，這套系統就沒辦法讓一家店做生意 → **一定要做**
- **進階（延伸細項）** = 拿掉它，系統還能運作，只是比較笨 → **基礎全綠燈後才動工**

**八大模組全部在基礎層。** 分層是在模組「裡面」切，不是砍掉整個模組。

## 2. 推進方式：廣度優先

先把八個模組的核心細項各做最薄一層、串成一條營運閉環，再回頭一層層加厚。
**不是**做完模組 A 才做模組 B。

---

## 3. 基礎功能總表

### M1 [菜單與品項](modules/M1-菜單與品項.md)　★★★☆☆

| # | 功能 | 狀態 |
|---|---|---|
| 1.1 | 分類 CRUD | ☐ |
| 1.2 | 品項 CRUD（名稱、描述、價格、照片、分類） | ☐ |
| 1.3 | 上下架 | ☐ |
| 1.4 | 選項群組模型（份量／口味／加購，可跨品項共用） | ☐ |
| 1.5 | 必選驗證（`min_select` / `max_select`） | ☐ |
| 1.6 | 鍋底標記（每桌至少一份） | ☐ |
| 1.7 | 鴛鴦鍋（兩個選項群組表達，不建鍋位表） | ☐ |
| 1.8 | 售完標示 | ☐ |
| 1.9 | 免費續加品項（0 元、不扣庫存、仍要出單） | ☐ |
| 1.10 | 飲食標示（素食／不含牛／辣度 0–3） | ☐ |
| 1.11 | 菜單查詢 API | ☐ |
| 1.12 | 飲料與酒分類（一般分類，不做年齡驗證） | ☐ |


### M2 [桌位、開桌與候位](modules/M2-桌位開桌與候位.md)　★★★☆☆

| # | 功能 | 狀態 |
|---|---|---|
| 2.1 | 桌位主檔（桌號、座位數、區域、可否預約） | ☐ |
| 2.2 | 桌位狀態機（空桌／用餐中／待清理，三種） | ☐ |
| 2.3 | QR code 產生與列印（內容固定為 `/t/{tableNo}`） | ☐ |
| 2.4 | 掃碼進入（依桌位狀態顯示三種畫面） | ☐ |
| 2.5 | 開桌（三種來源：現場填人數／候位／預約，同一頁三頁籤；目標桌預約保留中回 `409 TABLE_RESERVED`；115 分鐘內有別筆訂位 → 強烈提醒＋「仍要開桌」二次確認，只提醒不擋） | ☐ |
| 2.5b | 開桌來源清單（可報到的候位與預約） | ☐ |
| 2.5c | 來源狀態收尾（候位／預約轉 `SEATED`） | ☐ |
| 2.5d | 從人出發開桌（S-07b／S-10b 依人數選空桌） | ☐ |
| 2.6 | 顧客加入（取得用餐權杖與同桌身分 `guest`，給共用購物車標示誰加的） | ☐ |
| 2.7 | 關桌（權杖立刻失效；沒有「結帳中」桌態） | ☐ |
| 2.7b | 清潔完成（確認框後回空桌） | ☐ |
| 2.8 | 桌況總覽（即時；進入保留窗的桌顯示第四種色塊「預約保留」，見 7.10） | ☐ |
| 2.9 | 用餐計時與超時標示 | ☐ |
| 2.10 | 候位登記取號 | ☐ |
| 2.11 | 候位清單與叫號 | ☐ |
| 2.12 | 候位報到開桌（選一張坐得下的空桌） | ☐ |
| 2.13 | 候位過號（排程自動標記） | ☐ |
| 2.14 | 取消該次用餐（僅店長；沒點過餐才可以，否則 `409 SESSION_HAS_ORDERS`；桌位直接回空桌、同桌手機直接回首頁） | ☐ |

### M3 [點餐與服務鈴](modules/M3-點餐與服務鈴.md)　★★★★☆

| # | 功能 | 狀態 |
|---|---|---|
| 3.1 | 購物車（**存後端、整桌共用**，加入時廣播 `CART_UPDATED`，每列標示誰加的；自己的動作不通知自己） | ☐ |
| 3.2 | 送出整桌點餐（不帶品項，整份購物車變一張 `order_ticket`；購物車空的回 `409 CART_EMPTY`） | ☐ |
| 3.3 | 鍋底規則（本桌至少一份，送出時沒有就 `400 SOUP_BASE_REQUIRED`） | ☐ |
| 3.4 | 選項驗證 | ☐ |
| 3.5 | **庫存扣減與防超賣**（整張單原子性） | ☐ |
| 3.6 | 快照寫入（名稱、單價、選項） | ☐ |
| 3.7 | 多次加點（`sequence_no` 遞增） | ☐ |
| 3.8 | 本桌訂單查詢（依單分組 + 出餐狀態） | ☐ |
| 3.9 | **多裝置即時同步（WebSocket，1 秒內）** | ☐ |
| 3.10 | 取消單品／整單（庫存回補） | ☐ |
| 3.11 | 結帳後不可加點（列鎖 + `409 SESSION_CLOSED`） | ☐ |
| 3.12 | 服務鈴（服務生／加湯／換鍋撈渣，三種） | ☐ |
| 3.13 | 防重複送出 | ☐ |
| 3.14 | 三態處理（載入中／空／錯誤） | ☐ |

> 原 3.E4「誰加的」標示已併入 3.1（2026-09-16，購物車改成存後端、整桌共用之後就變成基礎），編號不重編。
> 不經過整桌購物車的只有兩種：櫃檯代客加點（S-03 直接出單，同桌通知顯示「櫃檯 送出 N 項」），以及 C-17 預先點餐（A2，購物車只存在那支手機）。
> 全站的「N 項」都是**份數加總**（白飯 ×2 算 2 項）：購物車角標、C-06「小計 N 項」、C-04c「送出 N 項」、C-07 每張單的件數、S-03「小計 N 項」都一樣，見 [01 名詞定義](01-專案總覽.md)。

### M4 [出菜看板 KDS](modules/M4-出菜看板.md)　★★★☆☆

| # | 功能 | 狀態 |
|---|---|---|
| 4.1 | 看板查詢（以點餐單為卡片） | ☐ |
| 4.2 | 即時推播（1 秒內） | ☐ |
| 4.3 | 逐項勾選（勾選框 ≥ 40×40px） | ☐ |
| 4.4 | 整單完成 | ☐ |
| 4.5 | 狀態自動推導（由品項推算單狀態） | ☐ |
| 4.6 | 久候警示（>15 分鐘） | ☐ |
| 4.7 | 出餐順序建議（鍋底 → 菜盤 → 肉品） | ☐ |
| 4.8 | 預約優先排序（**隨 M7 預先點餐一起做，基礎沒有預點就沒有這件事**） | ☐ |
| 4.9 | 退菜紀錄 | ☐ |
| 4.10 | 深色 UI（全站唯一） | ☐ |
| 4.11 | 大字級（桌號 ≥32px、品項 ≥20px） | ☐ |
| 4.12 | 顧客端出餐進度同步 | ☐ |

### M5 [庫存](modules/M5-庫存.md)　★★★★☆

| # | 功能 | 狀態 |
|---|---|---|
| 5.1 | 品項層級庫存 | ☐ |
| 5.2 | **防超賣扣減**（條件式 UPDATE，看影響筆數） | ☐ |
| 5.3 | 告罄自動下架 | ☐ |
| 5.4 | 售完即時推播 | ☐ |
| 5.5 | 取消回補 | ☐ |
| 5.6 | 續加品不扣庫存（`track_inventory=false`） | ☐ |

> **5.7～5.10（手動補貨、異動紀錄、低水位警示、低庫存排程通知）移到進階**，見 §4.1。
> 那四項是店家端 S-09 那一頁的內容。**扣庫存與防超賣（5.1～5.6）留在基礎**——
> 它是整個專題技術含量最高的一段，而且 C-04 的「售完」是它算出來的，抽掉就沒東西可講。
> 編號沿用不重編。

### M6 [結帳](modules/M6-結帳.md)　★★☆☆☆

| # | 功能 | 狀態 |
|---|---|---|
| 6.1 | 帳單計算（小計 + 服務費 10%，`BigDecimal`） | ☐ |
| 6.2 | 帳單明細 | ☐ |
| 6.3 | 結帳防呆：金額比對（`expectedTotal` + `409 BILL_CHANGED`） | ☐ |
| 6.7 | 櫃檯結清（現金／信用卡／條碼三顆，按下後倒數 5 秒、可取消，S-03／S-03b） | ☐ |
| 6.9 | 桌位轉待清理 | ☐ |
| 6.10 | 權杖失效＋同桌手機顯示已結帳 | ☐ |
| 6.11 | 防重複送出（櫃檯結清鈕送出中停用） | ☐ |

> **基礎的結帳 = 客人到櫃檯結帳。** 6.4（取消結帳）刪除，空號不重編。
> 原 6.5、6.6、6.8、6.12 移到 6.E7（A5 顧客線上結帳），編號留空。

### M7 [訂位](modules/M7-訂位.md)　★★★★☆

| # | 功能 | 狀態 |
|---|---|---|
| 7.1 | 任意時間預約（`start_time` / `end_time`；只能訂明天起 30 天內） | ☐ |
| 7.2 | **區間重疊檢查**（`A.start < B.end AND B.start < A.end`；算佔用的是 `CONFIRMED`，以及 `SEATED` 且用餐紀錄還沒結帳的） | ☐ |
| 7.3 | best-fit 配桌 | ☐ |
| 7.4 | 可訂時段查詢 | ☐ |
| 7.5 | 併發控制（悲觀鎖） | ☐ |
| 7.6 | 唯一約束（資料庫層保險） | ☐ |
| 7.7 | 可預約比例 70% | ☐ |
| 7.8 | 客人線上取消（訂位時間前 30 分鐘前；之後回 `409 CANCEL_DEADLINE_PASSED`，請來電） | ☐ |
| 7.9 | 報到開桌（S-07b 選桌；最早提前 30 分，太早回 `409 RESERVATION_TOO_EARLY`，逾時回 `409 RESERVATION_EXPIRED`） | ☐ |
| 7.10 | **訂位保留桌位**（配到的桌在訂位前 30 分～後 10 分鎖住；**算出來的，不是排程**；要解開就是櫃檯代客取消訂位，見 7.18） | ☐ |
| 7.11 | 排程：NO_SHOW 標記（訂位保留 10 分鐘） | ☐ |
| 7.12 | 排程：訂位提醒 | ☐ |
| 7.13 | 店家端訂位管理 | ☐ |
| 7.14 | 排程手動觸發 API | ☐ |
| 7.15 | **訂位即成立**（送出直接 `CONFIRMED`，不經付款） | ☐ |
| 7.16 | 開桌時列出可報到的預約（給 S-02d，現在 −10～+30 分） | ☐ |
| 7.17 | 報到轉 `SEATED`（不動 `table_id`，本專題不做換桌） | ☐ |
| 7.18 | **櫃檯代客取消**（`POST /api/admin/reservations/{id}/cancel`；隨時可以取消、不受期限；不是 `CONFIRMED` 回 `409 RESERVATION_NOT_CANCELLABLE`；桌子與名額一起釋出） | ☐ |

> 7.10 原本是「排程：桌位預留轉換」，那支排程已經刪掉，**排程仍是九支**。2026-09-16 這個編號恢復使用，
> 改成**算出來的**訂位保留：桌位欄位一樣只有三種，查桌況時才判斷要不要顯示「預約保留」，不存桌態、不靠排程。
> 為什麼這樣就沒有舊版的同步問題，見 [M2 問題 5](modules/M2-桌位開桌與候位.md)。

> **預先點餐不在基礎裡。** 基礎交出來的是「選時間 → 訂位成立 → 到店報到開桌」這條線；
> 預點、事後調整、報到轉單整塊在進階（M7 的 7.E1～7.E3）。

### M8 [會員](modules/M8-會員.md)　★★★☆☆

| # | 功能 | 狀態 |
|---|---|---|
| 8.1 | 手機 OTP 註冊登入 | ☐ |
| 8.2 | 模擬簡訊（測試模式顯示驗證碼） | ☐ |
| 8.3 | 驗證碼規則（6 位、5 分鐘、錯 3 次作廢） | ☐ |
| 8.4 | 請求限流（10 分鐘 3 次） | ☐ |
| 8.5 | 補資料註冊（暱稱／性別／生日） | ☐ |
| 8.6 | JWT 發放與驗證 | ☐ |
| 8.7 | 會員中心 | ☐ |
| 8.11 | 員工帳號（三種角色、BCrypt） | ☐ |
| 8.12 | 權限控管（後端擋，不是前端藏） | ☐ |

> **8.8、8.9（消費歷史、單次消費明細）移到進階**，見 §4.1。那是 C-15 那一頁。
> **8.10（匿名轉會員）也移到進階**：基礎沒有點數也沒有消費紀錄，綁了會員看不出差別；它是 A1／A6 的前提。
> 8.10 有兩個入口：C-09 線上登入（A5），以及 S-03 櫃檯輸入手機查詢、唸生日確認後綁定（`GET /api/admin/members/lookup`）。
> 基礎的會員只剩登入、訂位免填資料、我的預約。會員中心（8.7）留在基礎，只是少兩個入口。編號沿用不重編。

---

## 4. 進階功能總表

> **基礎全部綠燈才動工。** 每一項都是獨立的，做不做都不影響基礎層運作。

### 4.1 本專題真的要切出去的七塊（2026-09-15 定案，09-16 補 A7）

下面這七塊是**已經畫了設計稿、但排在基礎後面**的：A1～A6 各有自己的畫面，共八張（C-09、C-10、C-11、C-15、C-17、C-20、S-09、S-12）；
A7 人氣推薦**沒有獨立畫面**，只是 C-04 菜單主頁裡的一塊。
其餘進階項目連稿都沒畫，是「未來規劃」等級；這七塊是「時間不夠就整塊不做，時間夠就照稿做」等級。

| # | 畫面 | 對應細項 | 拿掉的代價 |
|---|---|---|---|
| A1 | C-15 消費紀錄 | M8 8.8、8.9（8.10 匿名轉會員是前提） | 會員中心少一個入口，不影響訂位與點餐 |
| A2 | C-17 預先點餐／調整餐點 | M7 7.E1～7.E3、M4 4.8 | C-18 不再問「要先點餐嗎」、C-19 少一顆按鈕、KDS 少金框優先單 |
| A3 | S-09 庫存管理 | M5 5.7～5.10 | 補不了貨、看不到警示；扣庫存與防超賣仍是基礎 |
| A4 | S-12 報表 | M6 6.E2～6.E4 | 沒有營收數字可看，不影響任何一條營運動線 |
| A5 | C-09、C-10、C-11 顧客線上結帳（我要結帳） | M6 6.E7（原 6.5、6.6、6.8、6.12） | 客人不能在手機上付款，一律到櫃檯結帳；C-08 拿掉「我要結帳」按鈕，只留「用餐完畢請到櫃檯結帳」 |
| A6 | C-20 點數與優惠券（＋C-09 折抵區、C-11 點數列、C-14 入口列） | M8 8.E1、8.E2、M6 6.E5（8.10 是前提） | 沒有點數也沒有優惠券；基礎畫面上本來就不出現這兩樣 |
| A7 | 人氣推薦（C-04 的「本店人氣 TOP 5」區塊，沒有獨立畫面） | M3 3.E1（`GET /api/menu/recommendations?type=POPULAR`） | C-04 少一條橫向捲動的推薦卡片，整塊拿掉即可，不影響點餐；「推薦」頁籤與品項的「人氣」膠囊也來自推薦統計，一起拿掉，預設頁籤改「鍋底」 |

> **A6 的折抵規則**（2026-09-16）：優惠券與點數**可以同一次一起用**——先扣優惠券（一次最多一張）→ 再用點數（1 點折 1 元，最多折到 0 元）。
> 點數以**實付金額**計，每滿 100 元 1 點、無條件捨去（1,562 元 → 15 點），付款成功後才入帳。

> 畫面總數 43 張：顧客端 25（C-00、C-01、C-04、C-04b、C-04c、C-05～C-12、C-12b、C-13～C-16、C-16b、C-16c、C-17～C-21，C-02、C-03 空號）、
> 店家端 18（S-01、S-02、S-02b、S-02c、S-02d、S-03、S-03b、S-04～S-07、S-07b、S-08～S-10、S-10b、S-11、S-12），另有 8 張元件總表（DS-01～DS-08）。
> 進階畫面仍是 8 張；進階塊數 7（A7 沒有獨立畫面）。
> 頁面清單以 [10-UI-UX 規格](../ui/10-UI-UX規格.md) 的頁面表為準。

**刻意不列進階的四條線：**

| 留在基礎 | 理由 |
|---|---|
| 扣庫存與防超賣（M3 3.5、M5 5.1～5.6） | 整張單原子性、條件式 `UPDATE` 看影響筆數——這是最有得講的一段；C-04 的售完標示也靠它 |
| 選項群組管理畫面 S-06（M1 1.4／1.5 的後台） | 一度列進階（用 SQL 種資料就能 demo），2026-09-15 改回基礎 |
| 候位（M2 2.10～2.13）與服務鈴（M3 3.12） | 候位是開桌三頁籤的其中一條，抽掉 S-02c 要重畫；服務鈴是多裝置即時同步最好示範的一段，成本又低 |
| **櫃檯結帳**（S-03、S-03b，M6 6.1～6.3、6.7、6.9～6.11） | 客人預設到櫃檯結帳，這條不能砍——砍了整家店收不了錢 |

> **驗收怎麼講**：這七塊是「刻意排在後面」，不是「沒做完」。
> 簡報時直接說「基礎全綠燈，進階做了幾塊」，比含糊帶過好得多。

### 4.2 其餘進階項目（連稿都沒畫）

| 模組 | 進階項目 |
|---|---|
| M1 菜單 | 套餐組合、每日限量特餐、午晚餐不同菜單、圖片上傳、多語系 |
| M2 桌位 | 併桌拆桌換桌、候位自動遞補、預估等待時間、候位通知、樓層平面圖 |
| M3 點餐 | **人氣推薦（A7）**、個人化推薦、屬性推薦（「誰加的」標示已併入基礎 3.1） |
| M4 KDS | 分區出單、製作時間統計、**出單機列印佇列（RabbitMQ）**、叫號取餐 |
| M5 庫存 | **庫存管理畫面 S-09（A3）**、BOM 配方、備貨預測、叫貨統計、供應商進貨單、Redis 分散式鎖 |
| M6 結帳 | **報表 S-12（A4）**、**顧客線上結帳 C-09～C-11（A5）**、**優惠券與點數折抵（A6，可同一次一起用）**、發票模擬、翻桌率統計、熱門品項報表 |
| M7 訂位 | **預先點餐 C-17（A2）：不先結帳、事後可調整、報到轉單優先出單**、併桌組合建議、時間微調最佳化、訂金、加入行事曆 |
| M8 會員 | **消費紀錄 C-15（A1）**、**匿名轉會員 8.10（C-09 線上、S-03 櫃檯查手機兩個入口）**、**集點與優惠券 C-20（A6）**、會員分級、生日優惠、Refresh Token 輪替 |

---

## 5. 明確不做

以下**不在本次範圍**，有人動工請立即制止，並寫進 README 的「未來規劃」：

**因為系統定位（半輔助工具）**：找零計算、交班日結、現金核對、操作稽核紀錄、退菜折讓的財務流程、真實發票開立、分帳（想做再開 6.E6）、營業時間管理。

**因為範圍控制**：外帶／外送、多分店、吃到飽計價、真實金流串接、真實簡訊閘道、員工排班、酒類年齡驗證、多語系、深色模式（KDS 除外）、原生 App、細粒度權限設定。

---

## 6. 狀態機

> 圖見 [06-圖表總覽](06-圖表總覽.md)。以下為文字版規則，實作以此為準。

### 6.1 用餐紀錄 `DiningSession.status`

| 狀態 | 意義 | 可轉移到 |
|---|---|---|
| `OPEN` | 用餐中，可繼續點餐 | `PAID`、`CANCELLED` |
| `PAID` | 已結清（櫃檯標記，或進階 A5 線上付款成功） | （終態） |
| `CANCELLED` | 沒點餐就離開（店長在 S-03 取消該次用餐；送出過點餐單的不能取消，回 `409 SESSION_HAS_ORDERS`） | （終態） |

**只有 `OPEN` 可以建立新的 `OrderTicket`，也只有 `OPEN` 可以增刪改購物車**（否則 `409 SESSION_CLOSED`）。進入終態時該桌所有用餐權杖立刻失效，整桌購物車一起刪掉（還沒送出的品項直接捨棄，不算錢；櫃檯不用管購物車，S-03 不提示）。

兩種終態推同一個事件 `SESSION_CLOSED`，用 `reason` 分：

| `reason` | 桌位 | 顧客手機 |
|---|---|---|
| `PAID` | → `CLEANING` 待清理 | C-04 顯示「已結帳」面板，按「回到首頁」到 C-00 |
| `CANCELLED` | → 直接回 `AVAILABLE`（沒用過，不用清潔） | **不顯示任何面板**，清掉權杖直接導回 C-00（`/`） |

**「結帳時剛好有人加點」靠金額比對 + 列鎖處理，不靠狀態。** 送出點餐與結清／付款都在交易裡
`SELECT ... FOR UPDATE` 同一筆 `dining_session`，所以一定一先一後：
加點先 → 結清拿到 `409 BILL_CHANGED`（合計跟 `expectedTotal` 不同）；結清先 → 加點拿到 `409 SESSION_CLOSED`。
櫃檯與客人線上付款同時按，後到的也是 `409 SESSION_CLOSED`。細節見 [M6 問題 1](modules/M6-結帳.md)。

> 舊版還有一個 `CHECKOUT_PENDING`（要求結帳後鎖定不可加點，還要能取消），已經移除：
> 基礎是客人到櫃檯結帳，手機上沒有「開始結帳」這個時間點可以鎖。

### 6.2 點餐單 `OrderTicket.status`

`PENDING` → `PREPARING` → `SERVED`，任何階段可 → `CANCELLED`（庫存回補）。

**狀態由旗下 `OrderItem` 自動推導，不手動設定。**

### 6.3 桌位 `DiningTable.status`

| 狀態 | 可轉移到 | 誰觸發 |
|---|---|---|
| `AVAILABLE` 空桌 | `OCCUPIED` | 開桌（S-02 點空桌，或 S-07／S-10 報到開桌） |
| `OCCUPIED` 用餐中 | `CLEANING` | 櫃檯結清（或 A5 線上付款成功） |
| `OCCUPIED` 用餐中 | `AVAILABLE` | 店長取消該次用餐（沒點過餐，不用清潔） |
| `CLEANING` 待清理 | `AVAILABLE` | 櫃檯按清潔完成 |

**沒有「結帳中／待結帳」這種桌態**，任何畫面都不可以出現。

**桌位跟訂位有一個「算出來的」接點，跟候位沒有。**（2026-09-16 定案，推翻舊的「三套互不連動」）

`dining_table.status` 仍然只有上面三種，只反映現場當下；但查桌況時，`AVAILABLE` 的桌如果有一筆訂位同時符合
配到這張桌（`reservation.table_id`）、`CONFIRMED`、現在落在 `start_time − 30 分` ～ `start_time + 10 分`，
就**顯示成 `RESERVED`（預約保留）**：不能開給別人（`409 TABLE_RESERVED`）；要解開就是櫃檯代客取消那筆訂位（M7 7.10、7.18）。

| 顯示優先順序 | 條件 |
|---|---|
| 1 用餐中 | `status = OCCUPIED`（配到的桌到時間還在用餐中，就不顯示保留，櫃檯從 S-07b 幫客人選別張桌） |
| 2 待清理 | `status = CLEANING` |
| 3 預約保留 | `status = AVAILABLE` 且符合上面的保留條件 |
| 4 空桌 | 其他 |

**只有訂位會鎖桌，候位不鎖桌**：桌子會被鎖只有「預約保留」與「用餐中／待清理」兩種來源；候位叫號只是通知，客人到櫃檯才選桌開桌，客人不吃了就按「放棄」（`ABANDONED`），沒有東西要解鎖。另一個接點照舊：**開桌成功時，把來源那筆訂位或候位轉成已報到**（轉成 `SEATED` 之後保留條件不成立，保留自己消失）。

開桌有兩個入口：從桌子出發（S-02 點空桌 → S-02b／S-02c／S-02d），從人出發（S-07／S-10 → S-07b／S-10b），後端是同一組 API。

**開桌時稍後有訂位 → 強烈提醒，不擋**（所有開桌入口都一樣）：這張桌在「現在 ～ 現在 + 115 分」（用餐 100 分＋清桌 15 分）內有別筆 `CONFIRMED` 訂位要開始，
頂部顯示醒目警示條「A05 在 19:30 有訂位（陳小姐 4 位）。現在開桌，客人可能還沒吃完訂位就到了。」，主按鈕改成「仍要開桌」，按下再確認一次（「確定開桌」／「換一張桌」）；
S-07b／S-10b 清單裡這種桌標紅字「19:30 有訂位」，排在沒有衝突的桌後面。

舊版的 `RESERVED` 是**存在桌位欄位裡、靠排程在訂位前 30 分鐘轉入**的，cron 一沒跑就跟訂位對不上，已經移除；
現在的 `RESERVED` 只是顯示狀態，每次查詢現算，不存、不靠排程。
沒有「櫃檯自己把空桌鎖起來」這個功能（原本的延伸項目已刪除）。詳見 [M2 問題 5](modules/M2-桌位開桌與候位.md)。

### 6.4 預約 `Reservation.status`

`CONFIRMED` → `SEATED`，或 → `CANCELLED`（客人或櫃檯取消）／ `NO_SHOW`（排程標記）。

**報到最早在訂位時間前 30 分鐘**，更早按回 `409 RESERVATION_TOO_EARLY`「還沒到可報到時間（18:00 起）」。
**取消有兩個入口**，結果都是轉 `CANCELLED`、寫 `cancelled_at`、`table_id` 設 `NULL`（桌子與名額一起釋出），用 `cancel_source` 記是誰取消的：

| | 客人線上取消（7.8） | 櫃檯代客取消（7.18） |
|---|---|---|
| 期限 | **訂位時間前 30 分鐘**；之後回 `409 CANCEL_DEADLINE_PASSED`「訂位前 30 分鐘內請來電取消」 | **隨時可以取消**，不受任何期限 |
| 記錄 | `cancel_source = CUSTOMER` | `cancel_source = STAFF`、`cancelled_by_staff_id` |
| 不是 `CONFIRMED` | `409 RESERVATION_NOT_CANCELLABLE` | `409 RESERVATION_NOT_CANCELLABLE` |

取消期限剛好等於預約保留開始的時間，所以**保留開始之後只剩櫃檯能取消**。櫃檯取消是**單向**的，沒有恢復的 API，
按之前要二次確認「確定要取消 陳怡君 18:30 的訂位嗎？通常是客人來電取消。取消後 A08 會變回空桌，這筆訂位無法恢復。」。
C-19 我的預約：櫃檯取消的顯示「店家已代為取消」，客人自己取消的顯示「已取消」。

**取消期限和預點截止是兩條線**：客人線上取消到訂位時間前 30 分鐘；預點修改（進階 A2）仍到用餐日前一天 20:00。
以前兩者是同一個時間點，現在分開寫。**預點截止維持前一天 20:00**（2026-09-16 定案）。

**區間重疊只算 `CONFIRMED`，以及 `SEATED` 而且那筆用餐紀錄還沒結帳（`dining_session.status = OPEN`）的訂位。**
客人還坐著就要算，不然容量少算一組；結帳（`PAID`）或取消用餐之後就不算，名額立刻釋出，不用等到原本的 `end_time`。
桌位本身照舊：結清 → 待清理 → 清潔完成 → 空桌，接著給下一筆訂位的保留或現場、候位使用。
**線上訂位只能訂明天以後**，所以這條主要影響的是「同一天之後的訂位保留」與容量報表的正確性，不會出現當天被線上搶訂的情況。

**訂位保留 10 分鐘**：訂位時間過了 10 分鐘還沒報到就視為 `NO_SHOW`，只能現場候位。
排程每 5 分鐘跑一次，最多晚 5 分鐘才標，所以開桌頁的預約清單只列 `start_time` 在現在 −10～+30 分的，
報到 API 也自己檢查，逾時回 `409 RESERVATION_EXPIRED`。`NO_SHOW` 不用動桌位：預約保留在訂位時間後 10 分鐘就自己結束（它是算出來的，排程晚跑也不影響）。

顧客端 C-19 我的預約把 `NO_SHOW` 放在「已取消」頁籤，狀態膠囊「逾時未到」＋「需要的話請重新訂位」。

**沒有 `PENDING`**：選完時段送出就直接成立。舊設計的 `PENDING` 是為了「預付成功才確認」，
這一版訂位不收錢，這個狀態沒有任何路徑會產生，已經移除。

### 6.5 候位 `Waitlist.status`

`WAITING` → `CALLED` → `SEATED`，或 → `ABANDONED` / `EXPIRED`（叫號後 10 分鐘未到，排程自動）。

### 6.5b 預先點餐（進階，衍生狀態非欄位）

| 狀態 | 判定方式 |
|---|---|
| 未預點 | 該訂位沒有任何 `reservation_preorder_item` |
| 已預點 | 有內容，且現在 < 用餐日前一天 20:00 → **可改**（這是預點截止，跟取消期限「訂位前 30 分鐘」是兩條線，見 6.4） |
| 已鎖定 | 有內容，且已過上述時間 → 只能看，要改請來電 |
| 已轉單 | `reservation.status = SEATED`，內容已複製成 `order_ticket (is_preorder = 1)` |

### 6.6 付款 `Payment.status`

`PENDING` → `SUCCESS` → `REFUNDED`；`PENDING` → `FAILED`（可建立新的 `PENDING` 重試）。

- 櫃檯結清（基礎）直接寫一筆 `SUCCESS`，`method` 是 `CASH`／`CARD`／`BARCODE`（按下後倒數 5 秒的模擬流程，**不做失敗機率**）；沒有 `MOCK_PAY`
- A5 線上付款的 `method` 是 `APPLE_PAY`／`GOOGLE_PAY`／`LINE_PAY`
- `PENDING`／`FAILED` 只有進階 A5 的線上模擬付款才會出現
- `REFUNDED` 只給延伸 7.E7（訂金）用，**這一版沒有任何退款流程**

---

## 7. 非功能需求

| 項目 | 要求 | 驗證方式 |
|---|---|---|
| **效能** | 菜單頁首次載入 < 3 秒；一般 API < 500ms | Chrome DevTools Network |
| **即時性** | WebSocket 推播到畫面更新 < 1 秒 | 兩支手機並排測試 |
| **不超賣** | 最後一份被兩人同時點，只有一人成功 | **多執行緒測試 + Testcontainers** |
| **不重複訂位** | 兩人同時訂同一桌同時段，只有一人成功 | 同上 |
| **瀏覽器** | Chrome / Safari 最新版；**iOS Safari 必測** | 真機測試 |
| **RWD** | 顧客端 375px 起；店家端 1024px 起 | 實機 + DevTools |
| **可用性** | 內文 ≥16px、可點擊區 ≥44×44px（步進器等例外見 11）、對比度 ≥4.5:1 | 見 [11-設計系統](../ui/11-設計系統.md) |
| **安全** | 密碼 BCrypt；JWT 簽章；SQL 一律參數化 | Code review |
| **權限** | 每支 `/api/admin/**` 都有角色檢查 | **用 Postman 拿低權限 token 測，必須 403** |
| **錯誤處理** | 所有錯誤回統一格式，前端有對應提示 | 斷網／關 DB 測試 |
| **資料** | 種子資料含過去 90 天約 5000 筆單品 | 第 1 週完成 |
| **可觀測** | 所有排程任務有日誌；例外有完整堆疊 | 看 console |

---

## 8. 完成定義（每個功能都要符合）

- [ ] 已合併到 `develop`，且 `develop` 能正常啟動
- [ ] API 出現在 Swagger 上，參數說明正確
- [ ] 前端處理三態：載入中、空資料、錯誤
- [ ] 手機 375px 不破版（顧客端功能）
- [ ] 至少一位組員 review 過 PR
- [ ] 對應模組頁的驗收條件逐條測過
- [ ] 沒有殘留的 `console.log` / `System.out.println`
- [ ] 本頁的狀態欄已更新
