# 01 專案總覽

## 1. 專案定位

**一句話**：一套給中高價位、多人共鍋的台式火鍋店用的線上點餐系統。顧客掃桌上的 QR code 就能自己點餐、加點、按服務鈴，用餐完畢到櫃檯結帳（手機線上結帳是進階）；店家端有桌況、候位、出菜看板與庫存。

**專案代號**：`smart_order`

**系統定位**：**店家的半輔助工具，不是完整 POS。** 我們管「點什麼、出到哪、坐多久」，不管「錢怎麼進抽屜」。

## 2. 目標與非目標

### 目標

1. 做出一條**從掃碼到結帳完整走得通、不會壞**的顧客點餐流程
2. 店家端能即時看到桌況與出菜進度，並管理菜單、庫存、候位與訂位
3. 用**對的技術解決對的情境**（WebSocket 做即時、排程做定時、區間重疊做訂位），而不是繞路
4. 專案可作為五位成員的求職作品：有線上 demo、有 README、**每人能講出一個技術故事**

### 非目標

| 不做 | 原因 |
|---|---|
| 真實金流與簡訊串接 | 需商家申請與正式網域，對評分無幫助 |
| 找零、交班、日結、稽核、發票 | 系統定位是半輔助工具 |
| 外帶／外送、多分店 | 範圍控制 |
| 吃到飽計價 | 需額外的時限與加點規則 |
| 個人鍋 | 本店是共鍋形式 |
| 原生 App | 手機瀏覽器足夠 |

## 3. 使用者角色

| 角色 | 代號 | 裝置 | 說明 |
|---|---|---|---|
| 匿名顧客 | `GUEST` | 手機 | 掃 QR code 進入，不需註冊即可點餐 |
| 會員顧客 | `MEMBER` | 手機 | 訂位免填資料、查我的預約；消費紀錄（A1）、點數與優惠券（A6）、個人化推薦是進階 |
| 櫃檯人員 | `COUNTER` | 平板／桌機 | 桌況、開桌、結帳、候位、訂位查詢與取消預約保留 |
| 廚房人員 | `KITCHEN` | 平板 | 出菜看板 |
| 店長 | `MANAGER` | 桌機 | 菜單、庫存、報表。含以上所有權限 |

## 4. 技術棧

技術選型的**理由與取捨**見 [00-架構分層與技術選型](00-架構分層與技術選型.md) §3。
每項技術的**白話說明與學習資源**見 [技術總表](../tech/00-技術總表.md)。

### 後端

| 項目 | 選擇 | 版本 |
|---|---|---|
| 語言 | [Java](../tech/backend/12-Java與JDK.md) | 17 LTS |
| 框架 | [Spring Boot](../tech/backend/14-SpringBoot是什麼.md) | 3.x |
| 建置 | [Maven](../tech/backend/13-Maven.md) | Wrapper |
| 資料存取 | [Spring Data JPA](../tech/database/25-ORM與JPA.md) | 隨 Boot |
| 資料庫 | MySQL | 8.0 |
| **版本控管** | [**Flyway**](../tech/database/29-Flyway.md) | 隨 Boot |
| 認證 | [Spring Security](../tech/backend/20-SpringSecurity.md) + [JWT](../tech/backend/21-JWT.md) | jjwt |
| 即時 | [WebSocket + STOMP](../tech/realtime/41-WebSocket與STOMP.md) | SimpleBroker |
| 排程 | [`@Scheduled`](../tech/realtime/43-排程與Cron.md) | 隨 Boot |
| 快取／TTL | [Redis](../tech/realtime/44-Redis.md) | 7 |
| API 文件 | [springdoc-openapi](../tech/backend/19-Swagger.md) | 2.x |
| 測試 | [JUnit 5 + Mockito](../tech/quality/47-單元測試.md)、[Testcontainers](../tech/quality/48-Testcontainers.md) | |

### 前端

| 項目 | 選擇 | 限制 |
|---|---|---|
| 框架 | [React 18](../tech/frontend/34-React與JSX.md) | 函式元件 + Hooks only |
| 建置 | [Vite](../tech/frontend/33-Vite.md) | 不用 Next.js |
| 語言 | JavaScript | 不用 TypeScript |
| 樣式 | [Tailwind CSS 3](../tech/frontend/39-Tailwind.md) | 自建 10 個元件，不裝 UI 套件 |
| 路由 | [react-router-dom 6](../tech/frontend/38-ReactRouter.md) | 只用 Routes / Link / useNavigate |
| 狀態 | [`useState`](../tech/frontend/36-useState.md) + `useContext` | **禁用 Redux / Zustand** |
| 資料 | [`fetch`](../tech/frontend/40-fetch串接API.md) + [`useEffect`](../tech/frontend/37-useEffect.md) | **禁用 React Query / SWR** |
| 即時 | [`@stomp/stompjs`](../tech/realtime/42-前端接WebSocket.md) | 原生 WebSocket，不需要 sockjs-client |
| 圖示 | lucide-react | 統一一套 |

### 工具

| 用途 | 工具 |
|---|---|
| 版本控制 | [GitHub](../tech/team/04-GitHub與PR.md)（分支策略見 [05](05-開發流程與分工.md)） |
| UI 設計 | Figma + Figma Make（見 [12-AI UI 工具與提示詞](../ui/12-AI-UI工具與提示詞.md)） |
| API 測試 | Swagger UI ／ Postman |
| 本機環境 | [Docker Compose](../tech/team/05-Docker與Compose.md)（MySQL + Redis） |
| CI | [GitHub Actions](../tech/quality/49-GitHubActions.md) |
| 部署 | 後端 Render／Railway，前端 Vercel（見 [50-部署上線](../tech/quality/50-部署上線.md)） |

## 5. 系統架構

見 [06-圖表總覽](06-圖表總覽.md)。五層：使用者裝置 → 靜態主機 → 應用伺服器 → 模擬外部服務 → 資料層。

## 6. 專案結構

```
smart_order/
├── docs/                      ← 你現在看的文件
│   ├── spec/                  ← 規格文件
│   │   └── modules/           ← 八大模組規格
│   ├── tech/                  ← 50 頁技術教學
│   ├── ui/                    ← UI 文件
│   └── site/                  ← 產生出來的 HTML 版
├── backend/
│   ├── src/main/java/com/smartorder/
│   │   ├── SmartOrderApplication.java
│   │   ├── common/            ← 例外、統一回應、工具
│   │   ├── config/            ← Security、CORS、WebSocket、OpenAPI、Scheduler
│   │   ├── menu/              ← M1
│   │   ├── table/             ← M2（桌位、開桌、候位）
│   │   ├── order/             ← M3（點餐、服務鈴）
│   │   ├── kitchen/           ← M4
│   │   ├── inventory/         ← M5
│   │   ├── payment/           ← M6
│   │   ├── reservation/       ← M7
│   │   ├── member/            ← M8
│   │   └── report/            ← 統計與推薦查詢
│   └── src/main/resources/
│       ├── application.yml
│       └── db/migration/      ← Flyway V1__, V2__, ...
├── frontend-customer/         ← 顧客端 React
├── frontend-admin/            ← 店家端 React
├── docker-compose.yml
└── README.md
```

> **採用「依功能分包」**（`menu/MenuController.java`），不是「依技術分層」（`controller/MenuController.java`）。
> 前者在多人協作時衝突少很多，因為每個人動的檔案都在自己的資料夾裡。

## 7. 名詞定義（Glossary）

**這張表非常重要。** 程式碼命名、API 路徑、資料表名稱、口頭討論**一律以本表為準**。五個人對同一個東西用五個名字，是團體專題最常見的溝通災難。

| 中文 | 程式碼 | 定義 |
|---|---|---|
| 用餐紀錄／帳單 | `DiningSession` | 一桌從開桌到結帳的完整一次。結帳、集點、消費紀錄都以此為單位 |
| 購物車 | `CartItem` | **整桌共用、存在後端**（`cart_item`）。同桌每支手機看到同一份，誰都能增減、刪除；每列記下是誰加的 |
| 同桌客人 | `DiningSessionGuest` | 同一次用餐裡的「誰」：前端產生的裝置 UUID（`X-Device-Id`）＋顯示名稱（會員暱稱，或依加入順序「客人 1」「客人 2」）。**只用來標示誰加的，不是身分驗證**；同桌通知靠它的 id（`byGuestId`）判斷「是不是我自己」，自己的動作不通知自己 |
| 點餐單 | `OrderTicket` | 一次「送出整桌點餐」＝整份購物車變一張＝出單機印一張。一次用餐可以有多張 |
| 單品 | `OrderItem` | 點餐單裡的一個品項 + 數量 + 選項 |
| N 項（數量） | `quantity` 加總 | **N 項＝份數加總，不是列數**。白飯 ×2 算 2 項。購物車角標（`totalQuantity`）、C-06「小計 N 項」、C-04c「送出 N 項」（`NEW_TICKET` 的 `quantity`）、C-07 每張單的件數、S-03「小計 N 項」、預點的「已預先點餐 N 項」全部這樣算；畫面文字維持「項」 |
| 品項 | `MenuItem` | 菜單上的一道菜 |
| 分類 | `MenuCategory` | 鍋底／肉品／海鮮／蔬菜／火鍋料／主食／飲料／酒（與 [03](03-資料庫設計.md) 種子資料一致） |
| 選項群組 | `OptionGroup` | 一組可選規格（份量、辣度、加點），有最少／最多選幾個 |
| 選項 | `OptionValue` | 群組裡的一個選擇，可帶價差 |
| 鍋底 | Soup Base | `is_soup_base = true` 的品項。**每桌至少一份**（共鍋）。在 C-04 是一個分類頁籤，送出訂單時沒有鍋底回 `400 SOUP_BASE_REQUIRED` |
| 鴛鴦鍋 | — | 一個品項掛兩個 `min=1 max=1` 群組（左鍋／右鍋） |
| 免費續加 | Refill | `price = 0` 且 `track_inventory = false`，但仍要出單 |
| 桌位 | `DiningTable` | 實體的一張桌子 |
| 用餐權杖 | `sessionToken` | 顧客端的 API 憑證，結帳後立刻失效 |
| 服務鈴 | `ServiceCall` | 呼叫服務生／加湯／換鍋撈渣（**沒有結帳**，結帳請到櫃檯） |
| 候位 | `Waitlist` | 現場排隊取號 |
| 訂位 | `Reservation` | 線上預訂某時段某桌；**只能訂明天起 30 天內**，當天不接線上訂位 |
| 預約保留 | `displayStatus = RESERVED` | 訂位配到的桌，在訂位時間前 30 分～後 10 分顯示「預約保留」，不能開給別人；要解開就是櫃檯代客取消那筆訂位（沒有「只解除保留」的功能）。**查桌況時算出來的，不存在 `dining_table.status`、不靠排程**（M7 7.10）。只有訂位會鎖桌，候位不鎖桌 |
| 代客取消 | `cancel_source = STAFF` | 客人來電說不來了，櫃檯在 S-07／S-02d 幫他取消訂位（M7 7.18）。**隨時可以取消**，不受期限；單向，取消後無法恢復。客人自己線上取消只到訂位時間前 30 分鐘（`cancel_source = CUSTOMER`） |
| 開桌強烈提醒 | `upcomingOnThisTable` | 要開的桌在「現在～現在 + 115 分」內有別筆訂位要開始，開桌頁顯示醒目警示、主按鈕改「仍要開桌」並再確認一次；**只提醒不擋** |
| 清桌緩衝 | Buffer | 訂位的 `end_time` 已包含的 15 分鐘 |
| 出菜看板 | KDS | 廚房看的畫面 |
| 已結清 | — | 櫃檯標記收款完成（現金／信用卡／條碼，按下後倒數 5 秒的模擬流程），`dining_session` 轉 `PAID`。**基礎的結帳就是這個** |
| 金額比對 | `expectedTotal` | 結清／付款時帶上畫面看到的合計，後端重算不一樣就回 `409 BILL_CHANGED`。取代舊的「結帳中」鎖定 |
| 訂位保留時間 | — | 訂位時間後 10 分鐘。逾時視同 `NO_SHOW`，只能現場候位；配到的桌的預約保留也到這時為止 |
| 最早報到 | — | 訂位時間前 30 分鐘。更早按報到回 `409 RESERVATION_TOO_EARLY`；預約保留也從這時開始 |

## 8. 環境與設定

| 環境 | 用途 | 資料庫 |
|---|---|---|
| `local` | 每人本機開發 | Docker 的 MySQL + Redis |
| `prod` | Demo 用 | 雲端 MySQL |

**設定值一律放 `application.yml`，不要散落在程式碼裡**：

| 設定 | 值 |
|---|---|
| 服務費率 | 10% |
| 用餐時長 | 100 分鐘 |
| 清桌緩衝 | 15 分鐘 |
| 可預約桌數比例 | 70% |
| 免費取消期限（客人線上取消） | 訂位時間前 30 分鐘（＝預約保留開始；之後請來電，由櫃檯代客取消，櫃檯取消不受期限） |
| 預點修改截止（進階 A2） | 用餐日前一天 20:00（跟取消期限是兩條線；**是否也改成訂位前 30 分鐘，待使用者決定**） |
| 訂位提醒 | 用餐前 2 小時 |
| 用餐超時警示 | 100 分鐘 |
| 候位過號 | 叫號後 10 分鐘 |
| 待清理提醒 | 待清理超過 15 分鐘 |
| 服務鈴冷卻 | 同一種呼叫 60 秒 |
| NO_SHOW 判定（訂位保留時間） | 訂位時間後 10 分鐘 |
| 開桌頁預約清單時間窗 | 現在 −10 分 ～ +30 分 |
| 預約保留開始（`reservation.hold-before-minutes`） | 訂位時間前 30 分鐘（＝最早可報到） |
| 預約保留結束（`reservation.hold-after-minutes`） | 訂位時間後 10 分鐘（＝訂位保留時間） |
| 開桌強烈提醒時間窗 | 現在 ～ 現在 + 115 分鐘（＝用餐時長 100 分＋清桌緩衝 15 分，兩個設定值相加，不另開設定） |
| 驗證碼效期 | 5 分鐘 |
| 驗證碼錯誤上限 | 3 次 |
| OTP 限流 | 10 分鐘 3 次 |
| 櫃檯付款倒數 | 5 秒，倒數中可取消；現金、信用卡、條碼三種都倒數 |
| 結帳完成提示 | 約 1.5 秒後自動關閉，回桌況總覽 |
| 點數回饋（進階 A6） | 以實付金額計，每滿 100 元 1 點、無條件捨去（1,562 元 → 15 點），付款成功後才入帳 |
| 點數折抵（進階 A6） | 1 點折 1 元，最多折到 0 元 |
| 折抵順序（進階 A6） | 先扣優惠券（一次最多一張）→ 再用點數；券與點數可以同一次一起用 |
| 模擬付款成功率（進階 A5） | 95% |
| KDS 久候警示 | 15 分鐘 |
| 時區 | `Asia/Taipei` |
