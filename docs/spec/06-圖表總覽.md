# 06 圖表總覽

> 本頁所有圖以 Mermaid 撰寫，在 HTML 版與 GitHub 上都會自動渲染成圖。
> **每張圖下方都附有圖例，請先看圖例再看圖。**

---

## 1. 產品心智圖

回答：「這個產品到底有哪些東西？」

```mermaid
mindmap
  root((火鍋店<br/>點餐系統))
    M1 菜單與品項
      分類與品項管理
      選項群組模型
      鍋底每桌必點
      鴛鴦鍋兩個群組
      免費續加零元品項
      飲食標示素食不含牛
      售完標示
      進階 套餐與限量
    M2 桌位開桌候位
      桌上固定QR
      櫃檯控制開桌
      掃碼加入取得權杖
      桌況即時總覽
      用餐計時與超時
      候位登記取號
      叫號與報到
      候位過號排程
      依人數一鍵開桌
      進階 併桌與遞補
    M3 點餐與服務鈴
      共享購物車
      送出成立點餐單
      防超賣扣庫存
      快照寫入
      多裝置即時同步
      取消單品
      服務鈴三種
      進階 推薦系統
    M4 出菜看板
      卡片式看板
      逐項勾選出餐
      狀態自動推導
      久候警示
      預約優先排序
      出餐順序建議
      深色大字介面
      進階 分區出單
    M5 庫存
      品項層級庫存
      防超賣條件更新
      告罄自動下架
      售完即時推播
      進階 手動補貨與異動紀錄
      進階 低水位警示與排程
      進階 BOM與備貨預測
    M6 結帳
      小計加服務費
      客人到櫃檯結清
      金額比對擋結帳時加點
      桌位轉待清理
      權杖失效
      進階 顧客線上結帳
      進階 報表
      進階 優惠券折抵與發票
    M7 訂位
      選完時段即成立
      任意時間預約
      區間重疊檢查
      best-fit配桌
      悲觀鎖防重複
      訂位保留十分鐘
      排程未到與提醒
      報到選桌開桌
      進階 預先點餐不先結帳
      進階 預點事後調整
      進階 報到轉單優先出菜
    M8 會員
      手機驗證註冊
      JWT登入
      訂位免填資料
      員工角色權限
      進階 消費紀錄
      進階 匿名轉會員
      進階 點數與優惠券
    技術地基
      Flyway版本控管
      統一例外處理
      Swagger文件
      種子資料五千筆
      WebSocket五個頻道
      排程九個任務
      Redis驗證碼與限流
      Docker與CI
      部署上線
```

**圖例**

| 標記 | 意義 |
|---|---|
| M1～M8 | 八大模組，**全部在基礎層** |
| 「進階 …」 | 該模組的延伸細項，基礎全綠燈後才動工 |
| 技術地基 | 不是功能，但沒有它五個人會互相踩 |

---

## 2. 基礎與進階分層圖

回答：「什麼一定要做？什麼是加分的？」

```mermaid
flowchart LR
    subgraph L0["技術地基 L0 · 第 1 週"]
        direction TB
        G1["Flyway<br/>資料庫版本控管"]
        G2["統一回應<br/>全域例外處理"]
        G3["Spring Security<br/>雙軌認證"]
        G4["Swagger<br/>API 文件"]
        G5["Docker Compose<br/>與種子資料"]
    end

    subgraph BASE["基礎層 · 八大模組的核心細項"]
        direction TB
        M1["M1 菜單與品項　★★★☆☆"]
        M2["M2 桌位開桌候位　★★★☆☆"]
        M3["M3 點餐與服務鈴　★★★★☆"]
        M4["M4 出菜看板　★★★☆☆"]
        M5["M5 庫存　★★★★☆"]
        M6["M6 結帳　★★☆☆☆"]
        M7["M7 訂位　★★★★☆"]
        M8["M8 會員　★★★☆☆"]
    end

    subgraph ADV["進階層 · 可拆卸 不做也不影響運作"]
        direction TB
        A1["A1 消費紀錄<br/>C-15"]
        A2["A2 預先點餐 不先結帳<br/>事後可調整 報到轉單優先出菜<br/>C-17"]
        A3["A3 庫存管理畫面<br/>補貨 異動 警示<br/>S-09"]
        A4["A4 報表<br/>S-12"]
        A5["A5 顧客線上結帳<br/>C-09 C-10 C-11"]
        A6["A6 點數與優惠券<br/>C-20"]
        AX["其餘 連稿都沒畫<br/>推薦系統 BOM 併桌 出單機"]
    end

    G1 --> M1
    G5 --> M8
    M7 --> A2
    M5 --> A3
    M6 --> A5
    M8 --> A6

    classDef base fill:#FBE8E3,stroke:#C8442E,color:#2B211C
    classDef found fill:#FCF0DC,stroke:#E8A33D,color:#2B211C
    classDef adv fill:#EFE9E1,stroke:#9C8E84,color:#6B5D54,stroke-dasharray: 4 3
    class M1,M2,M3,M4,M5,M6,M7,M8 base
    class G1,G2,G3,G4,G5 found
    class A1,A2,A3,A4,A5,A6,AX adv
    linkStyle 0,1,2,3,4,5 stroke:#9C8E84,stroke-width:2px
```

**圖例**

| 樣式 | 意義 |
|---|---|
| 黃色 | 技術地基——不是功能，但缺了五個人會互相踩 |
| 紅色 | **基礎層**：拿掉它，這套系統就沒辦法讓一家店做生意 |
| 灰色虛線 | **進階層**：拿掉它系統還能運作，只是比較笨。A1～A6 已經畫了設計稿（共 8 張畫面），時間夠就照稿做；「其餘」那一格連稿都沒畫 |
| ★ | 難易度，見 [00-架構分層](00-架構分層與技術選型.md) §2.4 |

**推進方式是廣度優先**：八個模組先各做最薄一層、串成一條營運閉環，再回頭加厚。**不是**做完 M1 才做 M2。

---

## 3. 系統工程架構圖

回答：「東西跑在哪裡、彼此怎麼講話？」由左到右分五層。

```mermaid
flowchart LR
    subgraph L1["① 使用者裝置"]
        direction TB
        M["顧客手機瀏覽器<br/>React + Tailwind"]
        T["店家平板 / 桌機<br/>React + Tailwind"]
    end

    subgraph L2["② 靜態主機 Vercel"]
        direction TB
        FC["frontend-customer"]
        FA["frontend-admin"]
    end

    subgraph L3["③ 應用伺服器 Render / Railway"]
        direction TB
        SEC["Spring Security<br/>JWT + 用餐權杖"]
        CTRL["Controller 層<br/>只接參數與回傳 DTO"]
        SVC["Service 層<br/>商業邏輯與交易邊界"]
        REPO["Repository 層<br/>Spring Data JPA"]
        WS["WebSocket Broker<br/>STOMP 五個頻道"]
        SCHED["排程器<br/>九個定時任務"]
        ADV["全域例外處理"]
        DOC["Swagger UI"]
        SEC --> CTRL
        CTRL --> SVC
        SVC --> REPO
        SVC --> WS
        SCHED --> SVC
        CTRL -.-> ADV
        CTRL -.-> DOC
    end

    subgraph L4["④ 模擬的外部服務 全部在本地"]
        direction TB
        PAY["付款 Mock 進階 A5<br/>95% 成功"]
        SMS["簡訊 Mock<br/>驗證碼回傳畫面"]
    end

    subgraph L5["⑤ 資料層"]
        direction TB
        DB[("MySQL 8.0")]
        RDS[("Redis<br/>驗證碼 限流 快取")]
        SEED["種子資料<br/>90 天約 5000 筆"]
    end

    M -->|"HTTPS 載入頁面"| FC
    T -->|"HTTPS 載入頁面"| FA
    M -.->|"REST JSON"| SEC
    T -.->|"REST JSON"| SEC
    M <-.->|"WebSocket wss"| WS
    T <-.->|"WebSocket wss"| WS
    REPO --> DB
    SVC --> RDS
    SVC --> PAY
    SVC --> SMS
    SEED -.-> DB

    classDef front fill:#FBE8E3,stroke:#C8442E,color:#2B211C
    classDef back fill:#FCF0DC,stroke:#E8A33D,color:#2B211C
    classDef data fill:#E4EFE7,stroke:#3F7A4E,color:#2B211C
    classDef mock fill:#EFE9E1,stroke:#9C8E84,color:#2B211C,stroke-dasharray: 4 3
    class M,T,FC,FA front
    class SEC,CTRL,SVC,REPO,WS,SCHED,ADV,DOC back
    class DB,RDS,SEED data
    class PAY,SMS mock
```

**圖例**

```mermaid
flowchart LR
    A["方框<br/>程式模組或服務"] --> B["實線箭頭<br/>主要資料流"]
    B -.-> C["虛線箭頭<br/>輔助或週期性"]
    C --> D[("圓柱<br/>資料庫")]
    classDef f fill:#FBE8E3,stroke:#C8442E,color:#2B211C
    classDef b fill:#FCF0DC,stroke:#E8A33D,color:#2B211C
    classDef m fill:#EFE9E1,stroke:#9C8E84,color:#2B211C,stroke-dasharray: 4 3
    classDef d fill:#E4EFE7,stroke:#3F7A4E,color:#2B211C
    class A f
    class B b
    class C m
    class D d
```

| 顏色 | 代表 |
|---|---|
| 淺紅 | 前端（React） |
| 淺黃 | 後端（Spring Boot） |
| 淺綠 | 資料層（MySQL、Redis） |
| 灰底虛線 | 模擬的外部服務，**不串接真實 API** |

**三個關鍵設計決定**

1. **前端拆成兩個獨立專案**，顧客端與店家端互不影響
2. **雙向的 WebSocket 連線**是即時性的來源，REST 負責初次載入與寫入
3. **付款與簡訊全是本地 Mock**，不需要任何外部帳號或正式網域

---

## 4. 建議時程甘特圖

```mermaid
gantt
    title 火鍋店點餐系統 六週開發時程
    dateFormat YYYY-MM-DD
    axisFormat %m/%d
    excludes weekends

    section 第0週 準備
    全員讀必修八頁技術文件  :crit, d0, 2026-09-14, 2d
    分工確認與建 repo       :a0, 2026-09-16, 1d
    全員環境安裝            :a1, 2026-09-16, 2d

    section 第1週 基礎建設
    全員 React 基礎練習      :crit, b1, 2026-09-21, 2d
    後端骨架與共用層         :b2, 2026-09-21, 3d
    Flyway 建表與種子資料     :crit, b3, 2026-09-21, 3d
    菜單查詢 API            :b4, 2026-09-24, 2d
    前端專案與共用元件       :b5, 2026-09-23, 3d
    UI 主視覺定調           :b6, 2026-09-23, 3d
    里程碑 走通第一條路      :milestone, m1, 2026-09-25, 0d

    section 第2週 點餐核心
    選項群組與菜單 API       :c1, 2026-09-28, 2d
    送出點餐與防超賣         :crit, c2, 2026-09-29, 4d
    開桌與用餐狀態 API       :c3, 2026-09-28, 3d
    金額計算服務            :c4, 2026-10-01, 2d
    菜單頁與購物車          :c5, 2026-09-28, 5d
    店家端登入與桌況頁       :c6, 2026-09-28, 5d
    CI 與部署技術驗證        :crit, c7, 2026-10-01, 2d

    section 第3週 主流程打通
    WebSocket 設定與握手驗證  :crit, e0, 2026-10-05, 2d
    KDS API 與出菜勾選       :e2, 2026-10-05, 3d
    櫃檯結清與金額比對       :e1, 2026-10-06, 2d
    本桌訂單即時同步頁面      :crit, e3, 2026-10-06, 3d
    桌位詳情結帳區與已結帳狀態 :e4, 2026-10-08, 2d
    KDS 看板與菜單後台       :e5, 2026-10-05, 5d
    里程碑 主流程全綠燈      :crit, milestone, m2, 2026-10-09, 0d

    section 第4週 訂位候位服務鈴
    訂位區間重疊與配桌       :crit, f2, 2026-10-12, 4d
    服務鈴後端與推播         :f1, 2026-10-12, 2d
    人氣推薦與出餐順序       :f3, 2026-10-14, 2d
    顧客端訂位與服務鈴頁面    :f4, 2026-10-12, 4d
    候位管理與訂位後台       :f5, 2026-10-13, 4d
    一鍵開桌與訂位保留十分鐘   :f6, 2026-10-14, 2d
    里程碑 訂位到報到全通    :milestone, m3, 2026-10-16, 0d

    section 進階 有餘力才做
    A2 預先點餐與事後調整     :done, x1, 2026-10-19, 4d
    A2 報到轉單與優先出菜     :done, x2, 2026-10-22, 2d
    A1 會員消費紀錄          :done, x3, 2026-10-21, 2d
    A3 庫存管理畫面          :done, x5, 2026-10-22, 2d
    A4 報表                 :done, x6, 2026-10-26, 3d
    A5 顧客線上結帳          :done, x7, 2026-10-20, 3d
    A6 點數與優惠券          :done, x8, 2026-10-23, 2d

    section 第5週 會員庫存排程部署
    庫存模組與售完推播       :g1, 2026-10-19, 3d
    會員 OTP 與 JWT         :g5, 2026-10-19, 3d
    九個排程與手動觸發 API    :g6, 2026-10-21, 2d
    單元測試補齊            :g3, 2026-10-21, 3d
    前端三態與 RWD          :g4, 2026-10-19, 4d
    部署上線                :crit, g2, 2026-10-19, 4d
    里程碑 線上可訪問        :crit, milestone, m4, 2026-10-23, 0d

    section 第6週 收斂 不寫新功能
    修 bug 與跨瀏覽器測試     :crit, h1, 2026-10-26, 3d
    防超賣多執行緒測試       :h6, 2026-10-27, 1d
    README 與架構圖         :h2, 2026-10-29, 1d
    功能 GIF 與影片         :h3, 2026-10-29, 1d
    上線最終版與備援準備      :crit, h4, 2026-10-30, 1d
    簡報製作與演練          :crit, h5, 2026-10-30, 2d
    里程碑 專題交付         :crit, milestone, m5, 2026-11-01, 0d
```

**圖例**

| 樣式 | 意義 |
|---|---|
| **紅色粗體長條** | 關鍵路徑，延遲會直接拖累整個專案 |
| 一般長條 | 可平行進行 |
| ◆ 菱形 | 里程碑，當天要能實際演示 |
| 空白 | 已排除週末 |

**五個絕對不能延誤的點**

1. **9/20 前**：全員讀完必修八頁、分工確認
2. **9/25**：walking skeleton 打通
3. **10/09**：主流程全綠燈（含 WebSocket 同步）← **最重要**
4. **10/23**：線上 demo 可訪問
5. **10/26 起**：一行新功能都不寫

---

## 5. UML 類別圖（領域模型）

```mermaid
classDiagram
    direction TB

    class Member {
        +Long id
        +String phone
        +String nickname
        +Gender gender
        +LocalDate birthDate
        +int points 進階A6
        +boolean profileCompleted
    }

    class Staff {
        +Long id
        +String username
        +String passwordHash
        +StaffRole role
    }

    class DiningTable {
        +Long id
        +String tableNo
        +int seats
        +TableStatus status
        +boolean reservable
        +boolean canAccommodate(int people)
    }

    class DiningSession {
        +Long id
        +String sessionToken
        +int adultCount
        +int childCount
        +SessionStatus status
        +BigDecimal subtotal
        +BigDecimal serviceFee
        +BigDecimal total
        +boolean isOrderable()
        +boolean hasSoupBase()
        +void recalculate()
        +void settle(PayMethod method, BigDecimal expectedTotal)
    }

    class OrderTicket {
        +Long id
        +int sequenceNo
        +TicketStatus status
        +boolean isPreorder
        +OrderSource source
        +LocalDateTime placedAt
        +void refreshStatusFromItems()
    }

    class OrderItem {
        +Long id
        +String itemNameSnapshot
        +BigDecimal unitPriceSnapshot
        +BigDecimal optionsPrice
        +int quantity
        +BigDecimal lineTotal
        +ItemStatus status
        +void markServed()
    }

    class OrderItemOption {
        +Long id
        +String groupNameSnapshot
        +String valueNameSnapshot
        +BigDecimal priceDelta
    }

    class ServiceCall {
        +Long id
        +CallType type
        +CallStatus status
        +LocalDateTime createdAt
        +void acknowledge(Staff staff)
    }

    class Waitlist {
        +Long id
        +String ticketNo
        +int partySize
        +WaitStatus status
        +LocalDateTime calledAt
        +boolean isExpired(int minutes)
    }

    class MenuCategory {
        +Long id
        +String name
        +int sortOrder
    }

    class MenuItem {
        +Long id
        +String name
        +BigDecimal price
        +boolean isSoupBase
        +boolean trackInventory
        +boolean isRefill
        +boolean soldOut
        +int spicyLevel
    }

    class OptionGroup {
        +Long id
        +String name
        +int minSelect
        +int maxSelect
        +boolean validate(List~Long~ selected)
    }

    class OptionValue {
        +Long id
        +String name
        +BigDecimal priceDelta
    }

    class Reservation {
        +Long id
        +LocalDateTime startTime
        +LocalDateTime endTime
        +int partySize
        +ReservationStatus status
        +boolean overlaps(LocalDateTime s, LocalDateTime e)
        +boolean isCancellable()
        +boolean isExpired(LocalDateTime now)
    }

    class Payment {
        +Long id
        +PayMethod method
        +BigDecimal amount
        +PayStatus status
        +String transactionNo
    }

    class InventoryItem {
        +Long id
        +int quantity
        +String unit
        +int lowThreshold
        +boolean isLow()
    }

    class InventoryLog {
        +Long id
        +int changeQty
        +int quantityAfter
        +ChangeReason reason
    }

    Member "0..1" --o "0..*" DiningSession : 可為匿名
    DiningTable "1" --o "0..*" DiningSession
    DiningSession "1" *-- "0..*" OrderTicket
    OrderTicket "1" *-- "1..*" OrderItem
    OrderItem "1" *-- "0..*" OrderItemOption
    DiningSession "1" *-- "0..*" ServiceCall
    Staff "0..1" --o "0..*" ServiceCall : 處理者
    Waitlist "0..1" --o "0..1" DiningSession : 報到後綁定
    MenuCategory "1" --o "0..*" MenuItem
    MenuItem "0..*" -- "0..*" OptionGroup : 多對多共用
    OptionGroup "1" *-- "1..*" OptionValue
    MenuItem "1" --o "0..*" OrderItem : 快照參照
    MenuItem "1" -- "0..1" InventoryItem
    InventoryItem "1" *-- "0..*" InventoryLog
    Reservation "0..1" --o "0..1" DiningSession : 報到開桌
    Member "0..1" --o "0..*" Reservation
    DiningTable "0..1" --o "0..*" Reservation
    DiningSession "1" --o "0..*" Payment
```

**圖例：UML 關係符號**

| 符號 | 名稱 | 意義 | 例子 |
|---|---|---|---|
| `*──` 實心菱形 | 組合 Composition | 強擁有，父刪子也消失 | `DiningSession *-- OrderTicket` |
| `o──` 空心菱形 | 聚合 Aggregation | 弱擁有，子可獨立存在 | `MenuCategory o-- MenuItem` |
| `──` 實線 | 關聯 Association | 一般關係 | `MenuItem -- OptionGroup` |
| `1` `0..1` `0..*` `1..*` | 多重性 | 兩端各有幾個 | `1 *-- 1..*` |
| `+` | public | | |
| `List~Long~` | 泛型 | 等同 `List<Long>` | |

**四個要看懂的重點**

1. **`DiningSession → OrderTicket → OrderItem` 是三層組合**，對應「一次用餐 → 多張點餐單 → 多個品項」。這是整個系統的骨幹。
2. **`MenuItem` 與 `OptionGroup` 是多對多**，「辣度」建立一次可掛在 20 個品項上。鴛鴦鍋也是靠這個機制（掛兩個群組）表達，**不需要「鍋位」類別**。
3. **`OrderItem` 存快照**（`itemNameSnapshot` / `unitPriceSnapshot`），改菜單價格不影響歷史帳單。
4. **`ServiceCall` 和 `Waitlist` 是新增的兩個類別**，分別支撐服務鈴與現場候位。

---

## 6. UML 狀態圖

### 6.1 用餐紀錄 DiningSession

```mermaid
stateDiagram-v2
    direction LR
    [*] --> OPEN : 櫃檯開桌
    OPEN --> OPEN : 送出新的點餐單 鎖列後確認仍是 OPEN
    OPEN --> PAID : 櫃檯結清 或 進階 A5 線上付款成功 金額比對通過
    OPEN --> CANCELLED : 客人未消費離開
    PAID --> [*]
    CANCELLED --> [*]

    note right of OPEN
        只有這個狀態
        可以建立新的點餐單
        沒有結帳中狀態
        加點與結清搶同一列的鎖
    end note
    note right of PAID
        進入終態時
        該桌所有用餐權杖立刻失效
        推 SESSION_CLOSED 給同桌手機
    end note
```

> **沒有 `CHECKOUT_PENDING`。** 舊版「要求結帳 → 鎖定 → 可取消」那一段整個拿掉了，基礎是客人到櫃檯結帳。
> 「結帳時剛好有人加點」改用金額比對：結清／付款帶 `expectedTotal`，重算不相等回 `409 BILL_CHANGED`，
> 狀態留在 `OPEN`，刷新後再按一次；已經不是 `OPEN` 回 `409 SESSION_CLOSED`。A5 付款失敗一樣留在 `OPEN`。

### 6.2 點餐單 OrderTicket

```mermaid
stateDiagram-v2
    direction LR
    [*] --> PENDING : 顧客送出購物車
    PENDING --> PREPARING : 任一品項開始出餐
    PREPARING --> SERVED : 全部品項出餐完畢
    PENDING --> CANCELLED : 取消 庫存回補
    PREPARING --> CANCELLED : 取消 庫存回補
    SERVED --> [*]
    CANCELLED --> [*]

    note right of PREPARING
        狀態由旗下 OrderItem
        自動推導 不手動設定
    end note
```

### 6.3 桌位 DiningTable

```mermaid
stateDiagram-v2
    direction LR
    [*] --> AVAILABLE
    AVAILABLE --> OCCUPIED : 開桌 現場 候位 預約 或從 S-07b S-10b 一鍵開桌
    OCCUPIED --> CLEANING : 櫃檯結清 或 A5 付款成功
    CLEANING --> AVAILABLE : 櫃檯確認整理完成

    note right of AVAILABLE
        只有三種狀態
        桌位只反映現場當下
        誰訂了哪張桌存在 reservation
        開桌時才查出來提示
        沒有結帳中桌態
    end note
```

> **三種狀態、三個動作，剛好對得起來**：空桌可以開、用餐中可以看單與結帳、待清理可以按完成。
> 桌位系統跟訂位、候位是三套獨立的東西：訂位與候位各自管自己的狀態，桌位只反映現場當下。
> 兩邊只有一個接點——**開桌成功時，把來源那筆訂位或候位轉成已報到**。
>
> 舊版還有一個 `RESERVED`，由排程在訂位前 30 分鐘自動轉入，等於把 `reservation`
> 複製一份到桌位上、還要靠 cron 同步，已經移除。
> 櫃檯手動保留桌位是延伸功能（M2 的 2.E5），基礎不做。

### 6.4 訂位 Reservation

```mermaid
stateDiagram-v2
    direction LR
    [*] --> CONFIRMED : 選完時段送出 當場成立
    CONFIRMED --> SEATED : 到店報到 開桌
    CONFIRMED --> CANCELLED : 前一日 20 點前取消
    CONFIRMED --> NO_SHOW : 排程 過了訂位時間 10 分未到
    SEATED --> [*]
    CANCELLED --> [*]
    NO_SHOW --> [*]

    note right of CONFIRMED
        沒有 PENDING
        舊流程要預付成功才確認
        這一版訂位不收錢
        送出就直接成立
    end note
    note right of NO_SHOW
        訂位只保留 10 分鐘
        逾時只能現場候位
        桌位不用動
    end note
```

> **訂位保留 10 分鐘。** 排程每 5 分鐘才跑一次，所以開桌頁的預約清單只列現在 −10～+45 分的，
> 報到 API 也自己檢查，逾時回 `409 RESERVATION_EXPIRED`，不等排程。
>
> 取消會把 `table_id` 設為 `NULL`，釋放 `UNIQUE(table_id, start_time)`，那個時段才能重新被訂走。
> `NO_SHOW` 發生時那個時段已經開始了，不需要釋放，`table_id` 留著當紀錄（跟 [03](03-資料庫設計.md) 一致）；
> 它本來就不算佔位（區間重疊只看 `CONFIRMED`），桌位狀態也完全不受影響。

### 6.4b 預先點餐（進階）

這不是資料庫欄位，是由「有沒有內容」加上「現在幾點」推導出來的。

```mermaid
stateDiagram-v2
    direction LR
    [*] --> 未預點 : 訂位成立
    未預點 --> 已預點 : 送出預點內容
    已預點 --> 已預點 : 整批覆蓋 改數量或加菜
    已預點 --> 未預點 : 清空
    已預點 --> 已鎖定 : 用餐前一日 20 點
    未預點 --> [*] : 報到 只是單純開桌
    已鎖定 --> 已轉單 : 報到 複製成正式點餐單 扣庫存
    已轉單 --> [*]

    note right of 已鎖定
        只能看不能改
        要改請來電
    end note
```

> **「已鎖定 → 已轉單」才扣庫存。** 預點階段完全不碰庫存，
> 所以到店可能有品項已售完，報到 API 會回一份 `soldOut` 清單給櫃檯。
> 取捨的理由見 [M7 問題 7](modules/M7-訂位.md)。

### 6.5 候位 Waitlist

```mermaid
stateDiagram-v2
    direction LR
    [*] --> WAITING : 現場登記取號
    WAITING --> CALLED : 櫃檯叫號
    WAITING --> ABANDONED : 客人放棄
    CALLED --> SEATED : 報到 自動開桌
    CALLED --> EXPIRED : 排程 叫號後 10 分未到
    SEATED --> [*]
    ABANDONED --> [*]
    EXPIRED --> [*]

    note right of CALLED
        只有 CALLED 狀態
        會被排程標記過號
    end note
```

**圖例**

| 符號 | 意義 |
|---|---|
| `[*] -->` | 起始狀態 |
| `--> [*]` | 終止狀態，不可再轉移 |
| 箭頭上的文字 | 觸發轉移的事件或條件 |
| 「排程」開頭的轉移 | 由定時任務自動觸發，**不是人為操作** |
| 自己指向自己 | 狀態不變但有動作發生 |

---

## 7. 資料庫 ER 圖

```mermaid
erDiagram
    MEMBER ||--o{ DINING_SESSION : "可為 NULL 匿名"
    MEMBER ||--o{ RESERVATION : "建立"
    DINING_TABLE ||--o{ DINING_SESSION : "使用"
    DINING_TABLE ||--o{ RESERVATION : "被預約"
    DINING_SESSION ||--o{ ORDER_TICKET : "包含多張"
    DINING_SESSION ||--o{ PAYMENT : "付款"
    DINING_SESSION ||--o{ SERVICE_CALL : "服務鈴"
    WAITLIST ||--o| DINING_SESSION : "報到後綁定"
    RESERVATION ||--o| DINING_SESSION : "報到開桌"
    RESERVATION ||--o{ RESERVATION_PREORDER_ITEM : "預先點餐 進階"
    RESERVATION_PREORDER_ITEM ||--o{ RESERVATION_PREORDER_OPTION : "選項快照"
    ORDER_TICKET ||--|{ ORDER_ITEM : "包含"
    ORDER_ITEM ||--o{ ORDER_ITEM_OPTION : "選項快照"
    MENU_CATEGORY ||--o{ MENU_ITEM : "分類"
    MENU_ITEM ||--o{ MENU_ITEM_OPTION_GROUP : "掛載"
    OPTION_GROUP ||--o{ MENU_ITEM_OPTION_GROUP : "被掛載"
    OPTION_GROUP ||--|{ OPTION_VALUE : "包含"
    MENU_ITEM ||--o{ ORDER_ITEM : "被點"
    MENU_ITEM ||--o| INVENTORY_ITEM : "庫存"
    INVENTORY_ITEM ||--o{ INVENTORY_LOG : "異動"
    STAFF ||--o{ SERVICE_CALL : "處理"

    MEMBER {
        bigint id PK
        varchar phone UK "登入帳號"
        varchar nickname "匿名綁定時可為空"
        varchar gender "供屬性推薦"
        date birth_date
        int points "進階 A6"
    }
    DINING_TABLE {
        bigint id PK
        varchar table_no UK "A01 B02"
        int seats
        varchar status "空桌 用餐中 待清理"
        tinyint reservable "70 percent 開放訂位"
    }
    DINING_SESSION {
        bigint id PK
        bigint table_id FK
        bigint member_id FK "可 NULL"
        varchar session_token UK "顧客端憑證"
        int adult_count "不參與金額計算"
        int child_count
        varchar status "OPEN PAID CANCELLED"
        decimal subtotal
        decimal service_fee "10 percent"
        decimal discount "進階 A6"
        decimal total
    }
    ORDER_TICKET {
        bigint id PK
        bigint dining_session_id FK
        int sequence_no "第幾單"
        varchar status
        tinyint is_preorder "KDS 排最前"
        datetime placed_at
    }
    ORDER_ITEM {
        bigint id PK
        bigint order_ticket_id FK
        bigint menu_item_id FK
        varchar item_name_snapshot "價格名稱快照"
        decimal unit_price_snapshot
        decimal options_price
        int quantity
        decimal line_total
        varchar status
    }
    ORDER_ITEM_OPTION {
        bigint id PK
        varchar group_name_snapshot
        varchar value_name_snapshot
        decimal price_delta
    }
    SERVICE_CALL {
        bigint id PK
        varchar type "SERVICE SOUP POT_CLEAN"
        varchar status "PENDING ACKNOWLEDGED DONE"
        datetime created_at
    }
    WAITLIST {
        bigint id PK
        varchar ticket_no "A012"
        int party_size
        varchar status "WAITING CALLED SEATED ABANDONED EXPIRED"
        datetime called_at "排程從這裡算過號"
    }
    MENU_ITEM {
        bigint id PK
        bigint category_id FK
        varchar name
        decimal price
        tinyint is_soup_base "鍋底 每桌至少一份"
        tinyint track_inventory "false 為免費續加"
        tinyint sold_out
    }
    OPTION_GROUP {
        bigint id PK
        varchar name "份量 辣度 左鍋 右鍋"
        int min_select "0 為非必選"
        int max_select "1 為單選"
    }
    OPTION_VALUE {
        bigint id PK
        varchar name
        decimal price_delta
    }
    RESERVATION {
        bigint id PK
        varchar reservation_no UK "預約編號 R-YYMMDD-流水號"
        datetime start_time "區間重疊判斷"
        datetime end_time "含 15 分清桌緩衝"
        int party_size
        varchar status "四種 沒有 PENDING"
        varchar access_token "UK 匿名訂位的憑證"
        datetime preorder_updated_at "NULL 表示沒預點"
    }
    RESERVATION_PREORDER_ITEM {
        bigint id PK
        bigint reservation_id FK
        varchar item_name_snapshot "預點當下的名稱"
        decimal unit_price_snapshot "報到轉單時照抄"
        int quantity
    }
    RESERVATION_PREORDER_OPTION {
        bigint id PK
        bigint reservation_preorder_item_id FK
        varchar value_name_snapshot
        decimal price_delta
    }
    INVENTORY_ITEM {
        bigint id PK
        bigint menu_item_id FK "UK 一對一"
        int quantity "防超賣的關鍵欄位"
        int low_threshold
    }
    PAYMENT {
        bigint id PK
        varchar method "櫃檯 CASH MOCK_PAY 線上三種為進階"
        decimal amount
        varchar status
        varchar transaction_no "MOCK 開頭"
    }
```

**圖例：ER 關係符號（烏鴉腳記法）**

| 符號 | 意義 |
|---|---|
| `\|\|` | 恰好一個 |
| `o\|` | 零或一個 |
| `\|{` | 一個或多個 |
| `o{` | 零或多個 |
| PK / FK / UK | 主鍵／外鍵／唯一鍵 |

讀法範例：`ORDER_TICKET ||--|{ ORDER_ITEM` 讀作「**一張**點餐單包含**一個以上**的單品」。

**兩個關鍵約束**

```sql
-- 防重複訂位（資料庫層的保險）
CREATE UNIQUE INDEX uk_reservation_table_start ON reservation (table_id, start_time);

-- 區間重疊檢查用
CREATE INDEX idx_reservation_table_time ON reservation (table_id, start_time, end_time);

-- 匿名訂位的存取憑證（沒有 JWT 的客人靠它回來改自己的單）
CREATE UNIQUE INDEX uk_reservation_access_token ON reservation (access_token);
```

---

## 8. 主流程時序圖

### 8.1 店內用餐：掃碼 → 點餐 → 出菜 → 櫃檯結帳

**這就是 demo 當天要演的那條路。**

```mermaid
sequenceDiagram
    autonumber
    actor G as 顧客手機 A
    actor G2 as 顧客手機 B
    participant FE as React 顧客端
    participant API as Spring Boot
    participant WS as WebSocket Broker
    participant DB as MySQL
    participant KDS as 廚房看板
    actor CT as 櫃檯 S-03

    Note over G,KDS: 階段一 進場

    G->>API: 掃碼 GET /tables/A03/status
    API-->>G: AVAILABLE 請洽櫃檯帶位
    Note over API: 櫃檯開桌 悲觀鎖防重複
    G->>API: 再掃一次 POST /dining-sessions/join
    API-->>G: sessionToken + soupBaseOrdered false
    G->>FE: 存 localStorage 進 C-04 菜單 鍋底是其中一個頁籤
    G2->>API: 同一張 QR 也能加入
    API-->>G2: sessionToken

    G->>WS: 訂閱 /topic/session/1052
    G2->>WS: 訂閱 /topic/session/1052
    KDS->>WS: 訂閱 /topic/kitchen

    Note over G,KDS: 階段二 點餐 共鍋每桌至少一份鍋底

    G->>API: POST /orders 鴛鴦鍋底 + 牛五花
    activate API
    API->>DB: 開始交易
    API->>DB: 鎖住 session 列 檢查 OPEN 與鍋底規則
    API->>DB: UPDATE inventory SET qty=qty-n WHERE qty>=n
    alt 庫存足夠
        DB-->>API: 影響筆數 1
        API->>DB: 建立 ticket item option 含快照
        API->>DB: 提交交易
        API-->>G: 201 第 1 單已送出
        Note over API: 交易提交後才推播
        API->>WS: NEW_TICKET
        WS-->>G2: 新單出現 高亮 2 秒
        WS-->>KDS: 卡片跳出
    else 庫存不足
        DB-->>API: 影響筆數 0
        API->>DB: 全部回滾
        API-->>G: 409 ITEM_SOLD_OUT
        Note over G: 標紅該品項 整張單未成立
    end
    deactivate API

    Note over G,KDS: 階段三 出菜

    KDS->>API: PATCH /order-items/8801/serve
    API->>DB: 品項改 SERVED 推導單狀態
    API->>WS: ITEM_SERVED
    WS-->>G: 鴛鴦鍋底 已出餐
    WS-->>G2: 同步更新

    G->>API: POST /service-calls type SOUP
    API->>WS: SERVICE_CALL 到 /topic/counter
    Note over WS: 櫃檯畫面立刻跳出

    Note over G,CT: 階段四 結帳 客人到櫃檯 基礎

    CT->>API: GET /admin/dining-sessions/1052
    API-->>CT: 小計 服務費 10% 合計 1562
    CT->>API: POST /admin/dining-sessions/1052/settle CASH expectedTotal 1562
    activate API
    API->>DB: 同一交易 SELECT FOR UPDATE 鎖住 session 重算合計
    alt 仍是 OPEN 且合計相同
        API->>DB: Payment SUCCESS Session PAID 桌位 CLEANING 權杖失效
        API->>DB: 提交交易
        API-->>CT: 200 已結清
        API->>WS: SESSION_CLOSED
        WS-->>G: C-04 顯示已結帳 清除權杖
        WS-->>G2: 同步顯示已結帳
    else 手機 B 的加點先拿到鎖 合計變了
        API-->>CT: 409 BILL_CHANGED 附最新帳單
        Note over CT: 刷新金額後再按一次
    end
    deactivate API
    Note over G2,API: 結清先拿到鎖的話 晚到的加點回 409 SESSION_CLOSED

    opt 進階 A5 客人自己在手機上結帳
        G->>API: POST /payments LINE_PAY expectedTotal 1562
        API-->>G: 同一套鎖與金額比對 成功後一樣推 SESSION_CLOSED
    end
```

**圖例**

| 符號 | 意義 |
|---|---|
| 實線箭頭 `->>` | 發出請求 |
| 虛線箭頭 `-->>` | 回傳結果或推播 |
| `alt / else` | 分支：兩種可能的結果 |
| 灰色垂直長條 | 該參與者正在處理中（交易進行中） |
| `Note` | 補充說明 |

**這張圖的五個重點**

1. **掃碼第一次被擋下來**——桌位還是 `AVAILABLE`，櫃檯沒開桌。這是「半輔助工具」定位的具體呈現。
2. **第 11～24 步是整個系統最重要的交易**。扣庫存用 `WHERE qty >= n` 判斷影響筆數，任一項失敗整張單回滾。
3. **推播一律在交易提交之後**，否則前端收到通知去查卻查不到資料。
4. **手機 B 全程沒有發過一次查詢請求**，所有更新都是推過去的。但它連上時和斷線重連時，各要抓一次完整資料。
5. **結帳在櫃檯（第 32～42 步），沒有「結帳中」狀態。** 結清帶 `expectedTotal`，跟送出點餐（第 13 步）搶同一把列鎖：
   加點先到，結清拿到 `BILL_CHANGED`；結清先到，加點拿到 `SESSION_CLOSED`。客人手機上的「我要結帳」是進階 A5。

---

### 8.2 訂位與預先點餐：成立 → 補點 → 報到轉單

虛線框住的是**進階**（M7 的 7.E1～7.E3）。只做基礎的話，這張圖就是「查時段 → 訂位成立 → 報到開桌」三段。

```mermaid
sequenceDiagram
    autonumber
    actor G as 客人手機
    participant FE as React 顧客端
    participant API as Spring Boot
    participant DB as MySQL
    actor CT as 櫃檯 S-07
    participant KDS as 廚房看板

    Note over G,KDS: 階段一 訂位 基礎

    G->>API: GET /reservations/availability date partySize
    API-->>G: 各時段剩幾桌 客滿的標 false
    G->>API: POST /reservations 選好日期時段人數
    activate API
    API->>DB: 悲觀鎖候選桌 依 id 排序防死鎖
    API->>DB: 區間重疊檢查 加 best-fit 挑最小夠坐的桌
    alt 有空桌
        API->>DB: 寫入 reservation 狀態 CONFIRMED
        API-->>G: 201 預約編號 桌號 accessToken
        Note over G,FE: 直接跳 C-18 訂位完成 沒有付款頁
    else 客滿
        API-->>G: 409 SLOT_UNAVAILABLE
        Note over G: 回時段頁重選
    end
    deactivate API

    Note over G,KDS: 階段二 預先點餐 進階 可跳過

    G->>FE: C-18 問 要先點餐嗎
    alt 選 到店再點
        Note over G: 流程結束 到店掃碼再點
    else 選 先點餐
        G->>API: PUT /reservations/442/preorder 整份購物車
        API->>DB: 刪掉舊的 重建 preorder_item
        Note over API,DB: 不檢查庫存 不扣庫存 不產生 payment
        API-->>G: 200 預估金額 可修改到前一日 20 點
    end

    G->>API: 事後從 C-19 我的預約 再送一次 PUT
    API-->>G: 200 整批覆蓋 同一支 API
    Note over API: 過了前一日 20 點回 409 PREORDER_LOCKED

    Note over G,KDS: 階段三 到店報到 S-07 按報到開桌 S-07b 選桌

    CT->>API: GET /admin/tables/available partySize 4
    API-->>CT: 坐得下的空桌 小桌在前 稍後有預約的附時間
    CT->>API: POST /admin/reservations/442/seat tableId 5
    activate API
    alt 已過訂位時間 10 分鐘
        API-->>CT: 409 RESERVATION_EXPIRED 請改登記候位
    else 保留時間內
        API->>DB: 建立 dining_session 帶入人數
        opt 有預點內容
            loop 每一筆預點
                API->>DB: 扣庫存 WHERE qty 大於等於 n
                alt 扣得到
                    API->>DB: 複製成 order_item 價格快照照抄
                else 扣不到
                    Note over API: 挑進 soldOut 清單
                end
            end
            API->>DB: 建立 order_ticket is_preorder 等於 1
            API->>KDS: NEW_TICKET 金色邊框 排最前
        end
        API->>DB: reservation 改 SEATED 桌位改 OCCUPIED
        API-->>CT: 200 session 與 soldOut 清單
    end
    deactivate API
    Note over CT: soldOut 一定要顯示 當面跟客人講
```

**圖例**

| 符號 | 意義 |
|---|---|
| 實線箭頭 `->>` | 發出請求 |
| 虛線箭頭 `-->>` | 回傳結果或推播 |
| `alt / else` | 分支：兩種可能的結果 |
| `opt` | 可選：條件成立才發生 |
| `loop` | 重複執行 |
| `Note` | 補充說明 |

**這張圖的四個重點**

1. **第 7 步就結束了訂位。** 沒有付款、沒有等待確認，`201` 回來訂位就成立。
   舊流程要走完「選時段 → 先點餐 → 付款」三步才算訂到位，那是把訂位綁在金流上；
   拆開之後，客人少一道門檻，我們也少一整條退款分支要寫。
2. **預點是同一支 `PUT` 整批覆蓋**（第 10 步與第 13 步）。「先點餐」和「事後調整」在後端是同一件事，
   前端也是同一個畫面（C-17），只差有沒有帶入現有內容。
3. **庫存到第 20 步才扣**。預點不鎖庫存，代價是到店可能售完 → `soldOut` 清單。
   這是設計選擇不是 bug，要在驗收時講得出來。
4. **報到前先選桌（第 15～17 步）**。S-07b 列出坐得下的空桌，選好才開桌；
   過了訂位時間 10 分鐘直接回 `RESERVATION_EXPIRED`（第 18 步），不等 NO_SHOW 排程。
